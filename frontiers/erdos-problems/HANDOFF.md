# Overnight handoff: Erdős mission, 2026-06-06

No full-problem solve met the solve bar in this pass. The bankable lead result
remains the gate-verified publishable partial for #302: a strictly better
constructive upper bound with two independent exact checks. After the refocus,
#307 was handled in a separate dedicated thread. Its residual-port global-bound
review downgraded #307 from best near-term solve candidate to partial reduction
only. This thread stopped #307 work and did not touch #488 or #647.

## Full-solve bet triage, 2026-06-07

Status: active solve goal, no solve claim.

## #458 lcm-ratio bridge, 2026-06-08

Status: partial formal bridge, no solve claim.

`lean/Vela/Erdos/Erdos458.lean` now has two additional checked lemmas:
`gapBaseProduct_factorization_eq_contributingExponents_card_of_mem` and
`lcmUpto_factorization_eq_factorization_add_gapBaseProduct`. Together they
prove the primewise exponent-accounting bridge for the intended lcm-ratio
reduction: for each prime base in a consecutive-prime gap, `gapBaseProduct`
records exactly the lcm exponent jump once certified maximal exponents are
supplied. This narrows the remaining formal gap for #458 from the whole
all-bases lcm-ratio reduction to a finite product-ext step:
`lcmUpto (r - 1) = lcmUpto p * gapBaseProduct p r`.

Focused verification run:

```bash
cd lean && lake build Vela.Erdos458
```

This does not solve #458. The mixed-cluster obstruction remains after the
lcm-ratio equality is machine-checked.

The current full-solve portfolio was checked against the local Vela notes,
Lean modules, and live Erdős pages. The best near-term proof request is #1056:
prove unbounded collision multiplicity for the factorial residue sequence
`0!, 1!, ..., (p-1)! mod p`. The local fast witness verifier now confirms
examples through every `2 <= k <= 15`; the new draft
`examples/erdos-problems/attack/erdos1056-witnesses-k2-through-k15-draft.v1.json`
adds a locally verified `k=15` certificate at `p=584133523`, using the
`-1 mod p` factorial-residue class hinted by OEIS A230315. The artifact also
enforces minimum interval length `2`.
New Lean support `lean/Vela/Erdos/Erdos1056.lean` proves the factorial-residue
reduction: for `a <= b < p`, equality `a! == b! mod p` is equivalent to the
interval product `(a+1)...b` being `1 mod p`, with a fixed-shift corollary.
It now also proves `fixed_shift_factorial_modEq_iff_root` and
`fixedShiftPoly_roots_card_le`: for fixed shift `d`, the equality
`t! == (t+d)! mod p` is exactly the root condition for
`prod_{i=1}^d (X+i)-1` over `ZMod p`, and that polynomial has at most `d`
distinct roots over a prime field. The same module now proves the direct
finite-start count `fixed_shift_factorial_collision_card_le` and the finite
shift-set sum bound `fixed_shift_factorial_collision_sum_card_le`, so the
`<= sum(D)` edge-count part of the fixed-shift obstruction is kernel-checked.
It additionally proves the abstract graph lemma
`reachable_card_le_two_mul_two_mul_extra_card_add_one`: preserving label edges
plus `E` extra label-edges, with fibers of size at most two, gives reachable
sets of size at most `2*(2*E+1)`. The Wilson-specialized wrapper
`wilsonLabel_reachable_card_le_two_mul_two_mul_extra_card_add_one` applies this
to the concrete reflection labels `min(t,p-1-t)` on residues below `p`. The
sharper interface is now Lean-backed too:
`reachable_card_le_two_mul_extra_card_add_one_of_label_bound` and
`wilsonLabel_reachable_card_le_two_mul_extra_card_add_one_of_label_bound` prove
the sharp `2*(E+1)` vertex bound once the label-level component bound
`reachable labels <= E+1` is supplied. That label graph count is now
Lean-backed in closed-label form by
`connected_label_finset_card_le_extra_card_add_one`, with closed-label wrappers
`reachable_card_le_two_mul_extra_card_add_one_of_label_connected` and
`wilsonLabel_reachable_card_le_two_mul_extra_card_add_one_of_label_connected`.
The internal-component bridge is now Lean-backed too:
`label_connected_of_vertex_connected_in` turns vertex-internal reachability into
closed label connectivity. The direct fixed-shift route-obstruction theorem is
`wilsonLabel_fixedShift_component_card_le_two_mul_sum_add_one`: a finite
component internally connected using Wilson-preserving steps and the collision
label-edges generated by a fixed shift set `D` has size at most
`2*(sum(D)+1)`.
This is still finite evidence only. The Agustín-Aquino/Hernández Santiago
tetrad construction is a real fixed-width Lean-formalized theorem, but it still
gives only four congruent factorials per prime, so the exact missing input is
an unbounded-width extension of that Wilson/factorial-congruence mechanism. A
GPT Pro prompt was sent for this specific theorem target. Prompt file:
`docs/erdos-attack/1056/gpt-pro-unbounded-collision-prompt.md`.
New route-obstruction artifact:
`examples/erdos-problems/attack/erdos1056-fixed-shift-obstruction-draft.v1.json`.
It records the intended route obstruction that Wilson-reflection plus a fixed
finite set `D` of shift-equality templates has method-graph components of size
at most `2*(1+sum(D))`, since each fixed shift `d` contributes at most `d` roots
of `prod_{i=1}^d(t+i)-1` over `F_p`. The Lean proof now has the root count,
label-edge count, Wilson fiber bound, closed-label graph count, and
internal-component bridge. This rules out only finite fixed-shift Wilson routes,
not #1056 itself.
The 2026-06-08 GPT-Pro pass added two sharper route obstructions without
solving the problem: a short-gap theorem showing that an `m`-point
factorial-residue class with all adjacent gaps `<= H` has
`m <= 1 + H*(H+1)/2`, and a saturated AP-root theorem blocking the natural
equal-length chain `a,a+d,...,a+d^2` for the fixed-shift polynomial. See
`docs/erdos-attack/1056/GPT_PRO_1056_OUTCOME_NOTE.md`.
The short-gap edge budget is now Lean-backed in
`fixedShiftCollisionStarts_shortGap_sum_card_le_triangular`: for prime `p` and
`H<p`, twice the total number of fixed-shift collision starts over gaps
`1..H` is at most `H*(H+1)`. The wrapper
`factorialResidueChain_shortGap_length_le_triangular` now formalizes the
indexed sorted-chain form: if a strictly increasing `m`-term factorial-residue
chain below `p` has all adjacent gaps at most `H`, then
`2*(m-1) <= H*(H+1)`. This proves the short-gap route obstruction in the Lean
kernel. It is not a #1056 solution because large-gap collision classes remain
uncontrolled.
The saturated AP-root route also gained Lean prerequisites:
`fixedShiftCorePoly`, `fixedShiftPoly_natDegree_eq`, `fixedShiftPoly_monic`,
`fixedShiftPoly_roots_card_eq_of_fin_roots`,
`fixedShiftPoly_prod_roots_eq_of_fin_roots`,
`fixedShiftPoly_roots_eq_of_fin_roots`,
`fixedShiftCorePoly_nextCoeff`, `fixedShiftPoly_nextCoeff_eq_sum`,
`fixedShiftPoly_nextCoeff_eq_neg_roots_sum_of_fin_roots`, and
`fixedShiftPoly_nextCoeff_eq_neg_fin_sum_of_fin_roots`,
`fixedShiftPoly_sum_range_eq_neg_fin_sum_of_fin_roots`,
`fixedShiftCorePoly_coeff_sub_two_eq_esymm`,
`fixedShiftPoly_coeff_sub_two_eq_esymm_range`,
`fixedShiftPoly_coeff_sub_two_eq_fin_roots_esymm`,
`fixedShiftPoly_esymm_range_eq_fin_roots_esymm_of_fin_roots`, and
`fixedShiftPoly_arithProgression_roots_coeff_constraints`; the AP algebra also
has `sum_range_succ_mul_two`, `sum_range_sq_mul_six`,
`sum_range_succ_sq_mul_six`, `arithProgression_fin_sum_mul_two`,
`arithProgression_fin_sum_sq_mul_six`,
`fixedShiftPoly_arithProgression_roots_sum_sq_eq`,
`fixedShiftPoly_arithProgression_roots_scaled_sum_eq`,
`fixedShiftPoly_arithProgression_roots_scaled_square_sum_eq`,
`ap_root_coeff_ring_obstruction`,
`fixedShiftPoly_arithProgression_roots_step_square_sub_one_mul_eq_zero`, and
`fixedShiftPoly_arithProgression_roots_step_square_eq_one_of_add_one_lt_prime`,
`natCast_mul_self_ne_one_of_two_lt_of_add_one_lt_prime`, and
`fixedShiftPoly_no_saturated_equal_length_chain_of_add_one_lt_prime`.
These show that a
positive fixed-shift polynomial over a prime field is monic of exact degree
`d`, that `d` distinct displayed roots saturate and equal the root multiset,
that the top coefficient comparison reduces to
`sum_{i<d}(i+1) = -sum_i c_i`, and that the next coefficient comparison reduces
to `esymm_2(1,2,...,d)=esymm_2(c_0,...,c_{d-1})`. The finite-sum extraction is
now kernel-checked: for roots `c_i=a+i*h`, the constraints imply
`d*(d-1)*(d+1)*(h^2-1)=0` in `ZMod p`, and if `2<d` and `d+1<p`, then
`h^2=1`. Thus saturated arithmetic-progression root families below the modulus
can only have step `±1`. The natural equal-length saturated chain is now ruled
out directly in Lean: `a+i*d` cannot all be roots under `2<d` and `d+1<p`,
because `(d:ZMod p)^2=1` would force `p | (d-1)(d+1)`. This blocks the natural
nontrivial AP-root route, but it is still not a #1056 solution because
arbitrary large-gap collision classes need not be saturated arithmetic
progressions.
The latest GPT-Pro pass adds another route obstruction and a sharper density
form. For `A_m(X)=prod_{i=1}^m(X+i)-1`, it gives proof-grade arguments that
`A_m` is irreducible over `Q`, that `gcd(A_m,A_n)=1` for `m<n`, and therefore
that `gcd(A_d(X),A_e(X+d))=1` for every fixed adjacent gap pair `d,e>=1`.
Thus fixed two-return characteristic-zero templates can occur only modulo
primes dividing the fixed nonzero resultant
`Res(A_d(X),A_e(X+d))`. A full solve now needs growing gap pairs and actual
linear common roots modulo prime divisors of those resultants. The same pass
also sharpens the local-density obstruction: if `b_d` counts adjacent gaps of
size `d` inside one factorial-residue class, then `b_d<=d`, so an `m`-point
class has diameter at least `(2*sqrt(2)/3+o(1))*(m-1)^(3/2)`. See
`docs/erdos-attack/1056/GPT_PRO_RESULTANT_ROUTE_OBSTRUCTION_NOTE.md` and use
`docs/erdos-attack/1056/gpt-pro-growing-resultants-next-prompt.md` for the next
external run.
The subsequent GPT-Pro pass makes the adjacent-pair obstruction quantitative:
if adjacent gaps `d,e` occur modulo prime `p`, then
`p <= 2^min(d,e)*(d+e)^(d*e)`. Hence for large `p`, no two consecutive gaps can
both be below about `sqrt(log p / log log p)`, and any proposed gap vector
must first pass the filter `p | gcd_i R_{g_i,g_{i+1}}` with `p > sum_i g_i`.
The latest GPT-Pro pass strengthens this from adjacent pairs to separated
interval pairs. If two product-one intervals of lengths `d,e` have start
separation `h>=d`, then `p` divides the nonzero separated resultant
`R_{d,e,h}` and
`p <= 2^min(d,e)*(h+e)^(d*e)`. Thus every pair of short intervals in one
collision chain must be separated by a large enough span; adding filler gaps no
longer evades the obstruction. See
`docs/erdos-attack/1056/GPT_PRO_SEPARATED_RESULTANT_OBSTRUCTION_NOTE.md`.
The same-length specialization is now banked too. For
`F_d(X)=prod_{i=1}^d(X+i)-1`,
`Res_X(F_d(X),F_d(X+Y)) = Y^d*Phi_d(Y)` with `Phi_d` monic of degree
`d*(d-1)`, so two non-overlapping intervals of length `d` separated by `h>=d`
force `p | Phi_d(h)` and
`h >= (1/4)*p^(1/(d*(d-1)))`. Repeated equal short gaps are therefore
polynomially separated. See
`docs/erdos-attack/1056/GPT_PRO_SAME_LENGTH_REPETITION_OBSTRUCTION_NOTE.md`.
The growing single-shift path route is now tighter too. GPT-Pro gives a
coefficient-elimination obstruction: for every `d>=4`, an equal-factorial chain
cannot contain `d-1` consecutive gaps all equal to `d`; for `d>=6`, `d-2`
consecutive gaps of length `d` force
`p | 5d^3-17d^2-5d-7`. The verifier
`scripts/verify_erdos1056_single_shift_run_obstruction.py` checks a bounded
box, verifies the quoted resultants, and now has an artifact replay mode:
`python3 scripts/verify_erdos1056_single_shift_run_obstruction.py --verify examples/erdos-problems/attack/erdos1056-single-shift-run-obstruction-draft.v1.json`.
The saved replay returns `status = verified`, `errors = []`,
`checked_theorem_a_cases = 9029710`, and
`checked_theorem_b_cases = 7930956`. See
`docs/erdos-attack/1056/GPT_PRO_SINGLE_SHIFT_RUN_OBSTRUCTION_NOTE.md`.
The newest GPT-Pro pass pushes this route further with claimed codimension
3, 4, and 5 eliminations: no `d-3`, `d-4`, or `d-5` consecutive equal gaps of
length `d` in the stated large-`d` ranges. The same verifier now records zero
finite violations for `d<=20`, `p<=3000`, with
`checked_codimension_cases = {3: 7420537, 4: 6329118, 5: 5818439}`. The
Groebner certificates have not been locally replayed. See
`docs/erdos-attack/1056/GPT_PRO_CODIMENSION_RUN_OBSTRUCTION_NOTE.md`.
The newest #1056 GPT-Pro pass adds a three-point metric obstruction: if
`0<=x<y<z<p` and `x! == y! == z! mod p`, then with
`D=y-x`, `E=z-y`, and `T=z-x`, one has
`p <= 2^min(D,E)*T^(D*E)`. Thus every second-neighbour span in a collision
class is at least `Theta(p)+1`. The verifier
`scripts/verify_erdos1056_three_point_metric_obstruction.py` checks all triples
from the saved `k=2..15` witnesses and all classes for primes `p<=1000`, with
zero violations. See
`docs/erdos-attack/1056/GPT_PRO_THREE_POINT_METRIC_OBSTRUCTION_NOTE.md`.
The newest #1056 GPT-Pro pass adds a short-gap sparsity wrapper: if
`2^H*(B+H)^(H^2) < p`, then starts of gaps `<=H` in a sorted collision class
are separated by more than `B`, so
`N_H <= 1+floor(S/(B+1))` and
`(L-2)*(H+1)*(B+1) <= S*(B+H+2)`. For repeated exact gap length `d`,
`(4B)^(d*(d-1)) < p` gives the same start separation. The verifier
`scripts/verify_erdos1056_short_gap_sparsity.py` checks the saved `k=2..15`
witnesses and all classes for primes `p<=1000`, with zero violations. See
`docs/erdos-attack/1056/GPT_PRO_SHORT_GAP_SPARSITY_NOTE.md`.
The newest #1056 GPT-Pro pass strengthens the separated-resultant route to a
multi-interval energy obstruction: any two disjoint product-one intervals in an
equal-factorial block force
`p <= 2^min(D,E)*W^(D*E)`, and adjacent gap vectors must satisfy the exact
pair-energy inequality
`binom(r,2)*log p <= log(2)*sum min(g_i,g_j)+sum g_i*g_j*log(G_ij)`.
This keeps the internal gap geometry and yields second-moment and bounded-gap
block obstructions. The verifier
`scripts/verify_erdos1056_multi_interval_energy.py` checks disjoint
interval-pair bounds, adjacent gap-pair bounds, and the logarithmic energy
inequality on the saved witnesses and all classes for primes `p<=1000`, with
zero violations. It still allows sparse large-gap constructions. See
`docs/erdos-attack/1056/GPT_PRO_MULTI_INTERVAL_ENERGY_NOTE.md`.
On the #1094 side, GPT-Pro's latest universal high-range pass gives a
prime-linear compression. Lean now proves `n_div_gcd_dvd_choose` and
`high_exception_n_div_gcd_prime`: in a high-range exception with `n>=k^2`,
`q=n/gcd(n,k)` is prime. Writing `k=g*h`, `n=g*q`, and `q=h*t+u`, every
remaining high exception must have `1<=u<h`, `gcd(u,h)=1`, `q` prime, and
`t | L_k/k`; the residual condition is `p ∤ C(g*q,g*h)` for every prime
`p<=t`. The verifier `scripts/verify_erdos1094_prime_linear_lcm_sieve.py`
checks `k<=80`, `t<=200000`, tests `2,395,779` prime-`q` candidates with the
exact Kummer condition, and finds only the known `(n,k)=(62,6)` survivor while
recording skipped large divisors. See
`docs/erdos-attack/1094/GPT_PRO_PRIME_LINEAR_LCM_NOTE.md`.
The broader binomial-divisor residue audit remains finite evidence only:
`scripts/verify_erdos1094_binomial_divisor_residue_sieve.py` checks the weaker
necessary condition `t | C(k-1,r)` through `k<=80` and records `30` survivor
rows. See `docs/erdos-attack/1094/GPT_PRO_BINOMIAL_DIVISOR_RESIDUE_NOTE.md`.
GPT-Pro then sharpened the broad target to a residual-prime block certificate.
Writing `N=k*t+r`, with `0<r<k` and `k<=t`, remaining candidates satisfy
`t | C(k-1,r)`. If `S_k(x)` is the full part of `x` supported on primes `<=k`
and `R_k(x)=x/S_k(x)`, the exact block condition is
`prod_{j=r-k+1}^r S_k(k*t+j)=k!`, with each residual `R_k(k*t+j)` equal to
`1` or a prime greater than `t`. The verifier
`scripts/verify_erdos1094_residual_prime_block.py` enumerates every divisor
`t | C(k-1,r)` for `0<r<k`, `k<=t`, with no `t` cutoff, through `k<=80`; it
checks `25,205,464` candidate divisors and finds only the known `(n,k)=(62,6)`
residual certificate. See
`docs/erdos-attack/1094/GPT_PRO_RESIDUAL_PRIME_BLOCK_NOTE.md`.
GPT-Pro then sharpened the target again to a positive-deficiency
prime-presentation certificate. For `N>=k^2`, write `N-s=a_s*b_s`, where
`a_s` is the full `k`-smooth part. The high-range condition is equivalent to
`prod_s a_s=k!` and `b_s=1` or `b_s` prime with `a_s<k`; the all-slot ELS
local divisor bound adds `a_s | (k-s)*C(k,s)` for every `1<=s<k`. The verifier
`scripts/verify_erdos1094_positive_deficiency_presentation.py` audits `k<=70`,
enumerates every divisor `t | C(k-1,r)` with no `t` cutoff, checks `8,212,269`
candidate divisors, and finds only `(n,k)=(62,6)`. See
`docs/erdos-attack/1094/GPT_PRO_POSITIVE_DEFICIENCY_PRESENTATION_NOTE.md`.
The current sharpest #1094 target is the full cap-residual tableau.
For `N=k*t+r`, `0<r<k`, `k<=t`, set
`A_s=gcd(N-s,k*C(k-1,s))` and `R_s=(N-s)/A_s`. A remaining high-range
candidate must have `t | C(k-1,r)`, `N/gcd(k,r)` prime,
`prod_s A_s=k!`, `gcd(R_s,L_k)=1` for every slot with
`L_k=lcm(1,...,k)`, and every `R_s` equal to `1` or prime with `A_s<k`.
Product-gcd equality alone is not sufficient; exact residual roughness is the
Kummer layer, and the residual prime/cap test is the final high-exception
layer. The verifier `scripts/verify_erdos1094_cap_rough_layer.py` audits
`k<=60`, checks `2,190,064` candidate divisors, and records `768` product-gcd
hits. Among those hits, `692` fail roughness, `30` of the remaining `76` fail
endpoint q-primality, `45` fail the final residual prime/cap layer, and only
`(n,k)=(62,6)` survives. The older q-prime-first verifier
`scripts/verify_erdos1094_gcd_prime_certificate.py`
audits `k<=69`, checks `7,182,793` candidate divisors, `823,218` q-prime cases,
and `407` product-gcd hits. Among those hits, `321` fail by composite residual,
`85` fail by a forced cap at least `k`, and the only full certificate is
`(n,k)=(62,6)`. See
`docs/erdos-attack/1094/GPT_PRO_CAP_RESIDUAL_TABLEAU_NOTE.md`.
The newest #1094 refinement replaces the cap table by the direct
`L_k`-saturated table. For `L_k=lcm(1,...,k)`, set
`S_s=gcd(N-s,L_k)` and `R_s=(N-s)/S_s`; the target is
`prod_s S_s=k!` and every residual is `1` or prime with `S_s<k`. One GPT-Pro
subclaim was corrected locally: `S_s=A_s` is not equivalent to roughness of
`(N-s)/A_s`, as the `k=7,r=2,t=15,N=107` counterexample has `A_s=S_s` but
residual `26`. The saturated final criterion still rejects that row. The
verifier `scripts/verify_erdos1094_lcm_saturated_table.py` audits `k<=60`,
checks `2,190,064` candidate divisors, records `768` saturated product hits,
rejects `684` by composite residual and `83` by `S_s>=k`, and leaves only
`(n,k)=(62,6)`. See
`docs/erdos-attack/1094/GPT_PRO_LCM_SATURATED_TABLE_NOTE.md`.
The newest centered form writes `M=k*t`. Lean now proves the center cap and
boundary lemmas, including `primeLinear_r_internal`: `t | C(k-1,r)` and
`t>=k` force `2<=r<=k-2`. In deficiency one, every candidate contains the
adjacent obstruction `M-1=A_-P_-`, `M+1=A_+P_+` with `P_-,P_+` prime and
`A_-,A_+<k`. Through `k<=69`, the verifier records `375` deficiency-one
product-gcd hits, `309` adjacent failures, `66` adjacent passes, and one full
certificate.
The combined local audit packet can be replayed with
`python3 scripts/verify_erdos1094_1056_packet.py --indent 2`; it verifies the
#1094 Kummer-CRT artifact, the #1094 prime-linear lcm sieve artifact, the
#1094 residual-prime block artifact, the #1094 positive-deficiency
presentation artifact, the #1094 cap-rough layer artifact, the #1094
cap-residual certificate artifact, the #1094 lcm-saturated table artifact, the
older #1094 `L_k/k` divisor-residue sieve artifact, the newer #1094
binomial-divisor residue sieve artifact, the #1056 single-shift run artifact,
the #1056
three-point metric artifact, the #1056 short-gap sparsity artifact, the #1056
multi-interval energy artifact, the #1056 k=2..15 witness artifact, and
`lake build Vela.Erdos1094Finiteness
Vela.Erdos1056`. The latest compact replay is saved at
`examples/erdos-problems/attack/erdos1094-1056-packet-replay-draft.v1.json`.
Replay that saved packet with
`python3 scripts/verify_erdos1094_1056_packet.py --verify examples/erdos-problems/attack/erdos1094-1056-packet-replay-draft.v1.json --indent 2`.

Other checked bets:

- #647: high-upside finite-witness problem, but current evidence is negative.
  The record dichotomy is exact: a first nontransitioning `h`-record gives the
  finite witness `n=m+tau(m)-2`. Existing artifacts verify no candidate through
  `3B`, and the remaining theorem is a global transition theorem for future
  divisor records, with residual small-divisor frontiers as the obstacle. New
  Lean cleanup in `lean/Vela/Erdos/Erdos647Obstruction.lean` proves
  `two_card_divisors_lt_of_seven_le` and the uniform
  `tau_equality_boundary`: the prime-channel ceiling `2*tau(k)=k+2` is reached
  only at `k in {2,4,6}`. This removes the previous artificial `k<=64`
  equality-check boundary but does not solve the record-transition problem.
  The record-dichotomy bookkeeping is now Lean-backed too via `hVal`,
  `HRecord`, `shiftedCandidate_iff_hVal_le`,
  `not_shiftedCandidate_of_record_transition`, and
  `shiftedCandidate_of_hRecord_no_transition`: a transition offset kills
  `n=hVal(m)-2`, while a large record holder with no transition offset is a
  `ShiftedCandidate`. This still leaves the global transition theorem or a
  first nontransitioning record as the hard obligation.
- #686: finite-counterexample route centered on `N=4`, but not a local close.
  New Lean module `lean/Vela/Erdos/Erdos686.lean` proves the `k=2` and `k=4`
  obstructions for every prime-power square root: if `A` is a prime power, then
  `A^2` has no two-factor or four-factor representation, so in particular
  `N=4` has no `k=2` or `k=4` representation. The `k=4` bridge is general:
  every `k=4` product equation reduces to a `k=2` equation with the same
  multiplier. Draft record:
  `examples/erdos-problems/attack/erdos686-k2-k4-prime-power-square-obstruction-record-draft.v1.json`.
  The same Lean module now also formalizes the all-`k` shift filter from the
  GPT Pro draft: with `intervalProd x k = product_{1<=i<=k}(x+i)`, if
  `intervalProd (n+d) k = 4 * intervalProd n k`, then
  `d | 3 * intervalProd n k`. This is useful pruning infrastructure, not an
  all-`k` obstruction. It now also proves the local disjoint-interval filter
  `intervalProd_local_shift_dvd`: if `intervalProd (n+d) k =
  C*intervalProd n k`, `k<=d`, and `1<=r<=k`, then
  `n+r | intervalProd (d-r) k`, i.e. each lower factor divides the shifted
  difference product `(d-r+1)...(d-r+k)`. The aggregate theorem
  `intervalProd_dvd_sourcePrefixProd_of_local_shift`, with full case
  `intervalProd_dvd_sourcePrefixProd_full_of_local_shift`, packages these
  pointwise divisibilities: `P_j(n)` divides the product of the shifted source
  intervals for `r=1..j`, and in particular `P_k(n)` divides the product of all
  shifted source intervals. This is proof infrastructure for future valuation
  or counting arguments, not a raw size contradiction. The new gap-product
  setup `gapIntervalProd`, `gapReverseProd`, `shifted_source_add_gap_factor`,
  `gap_factor_pos`, and `gapIntervalProd_last_factor` records the identity
  behind the transfer: after writing `x=n-d`, each source factor `d-r+j` pairs
  with `x+2*r-j` to sum to `n+r`. The signed congruence is now Lean-backed as
  `source_gapReverseProd_modEq_int`, and
  `lower_factor_dvd_gapReverseProd_of_source_dvd` transfers
  `n+r | P_k(d-r)` to `n+r | gapReverseProd(x,r,k)`. The reverse product is now
  proved equal to the interval-style gap product by
  `gapReverseProd_eq_gapIntervalProd`, giving
  `lower_factor_dvd_gapIntervalProd_of_source_dvd` and, in the remaining
  `k>=5` window, `lower_factor_dvd_gapIntervalProd_of_eq_four_ge_five`.
  Theorems `lower_factor_dvd_gapReverseProd_of_eq_four_ge_five` and
  `lower_prime_factor_le_gap_tail_of_eq_four_ge_five` prove the gap-tail prime
  bound `p<=n-d+2*r-1` for every prime divisor `p` of `n+r`. The bundled theorem
  `lower_prime_factor_le_dual_tail_of_eq_four_ge_five` gives
  `p<=min(d-r+k,n-d+2*r-1)`. This smoothness-only route is false as a complete
  obstruction: `scripts/verify_erdos686_dual_tail_counterexample.py` checks the
  state `k=5,d=11,x=12,n=23`, where all dual prime-factor bounds hold but exact
  local divisibility fails at `r=2` (`25 ∤ P_5(9)`). The next proof must use
  exact divisibility or valuations, not only prime-factor upper bounds. The
  exact source and gap filters are now packaged as
  `lower_factor_dvd_source_gap_gcd_of_eq_four_ge_five`:
  `n+r | gcd(P_k(d-r), gapIntervalProd(n-d,r,k))`. The obstruction theorem
  `no_eq_four_of_source_gap_gcd_lt_lower_factor` closes any row where this gcd
  is smaller than `n+r` for some `r`. The verifier
  `scripts/verify_erdos686_gcd_filter_prefix.py` found no states satisfying all
  gcd divisibility filters in the tested prefix `5<=k<=16`, `k<=d<=250`,
  `k<x<=250` (`691340` states checked). This is bounded evidence for a named
  theorem target, not a proof. The latest GPT Pro partial ruled out weaker
  global packages: `scripts/verify_erdos686_gptpro_partial.py` checks exact
  `k=5` counterexamples showing that size, separation, lower prime-freeness,
  shift divisibility, lower prime-factor smoothness, and even lower
  prime-power smoothness are not enough without exact local residue-aware
  divisibility. It also supplied an upper-interval analogue of the local
  filter, now Lean-backed as `upperReverseProd_eq_intervalProd`,
  `lower_upperReverseProd_modEq_int`,
  `upper_factor_div_gcd_dvd_lower_interval_of_eq_four`,
  `upper_factor_dvd_upperReverseProd_of_eq_four`,
  `upper_factor_dvd_upperIntervalProd_of_eq_four`, and
  `upper_quotient_prime_le_tail_of_eq_four`: each upper factor contributes
  `(n+d+s)/gcd(n+d+s,4)` to the opposite difference product
  `product_{j=1..k}(d+s-j)`. The upper prime-free consequence is now
  Lean-backed as `no_eq_four_of_upper_prime_ge_five` and
  `no_upper_block_prime_of_eq_four_ge_five`: every remaining separated
  `N=4,k>=5` row has no prime in `[n+d+1,n+d+k]`, so both interval blocks are
  prime-free. The exact residue-placement layer is now Lean-backed too:
  `prime_dvd_intervalProd_exists_term`,
  `lower_prime_factor_source_residue_of_eq`,
  `no_eq_of_lower_prime_factor_missing_source_residue`,
  `lower_prime_factor_gap_residue_of_eq_four_ge_five`,
  `no_eq_four_of_lower_prime_factor_missing_gap_residue`,
  `upper_prime_factor_residue_of_eq_four`, and
  `no_eq_four_of_upper_prime_factor_missing_residue`. The combined verifier
  `scripts/verify_erdos686_combined_local_filters.py` found no survivor in the
  remaining-window prefix `5<=k<=18`, `k<=d<=350`, `k<x<=350`, `d<2*x`
  (`1234639` states checked). Since every checked state already fails the
  lower source-gap gcd filter, the upper filter has not yet been needed
  computationally. The late first-failure examples are mostly a prime factor
  absent from both source and gap residue intervals; one valuation-only example
  is `k=5,d=5,x=8,r=3`, where `n+r=16` needs four powers of `2` and both exact
  products provide only three.
  The first extracted
  contradiction is also Lean-backed as `no_eq_four_of_lower_prime_gt_shift_tail`: if `n+r` is
  prime and `d-r+k < n+r`, then the `N=4` interval equation is impossible. The
  cleaner prime-gap corollary `no_eq_four_of_lower_prime_of_d_add_k_lt_n` says
  that if `d+k<n` and the lower block `[n+1,n+k]` contains a prime, then the
  equation is impossible. Equivalently, `lower_block_prime_forces_n_le_d_add_k`
  says any hypothetical solution whose lower block contains a prime must have
  `n<=d+k`. The near-diagonal branch is Lean-closed for `k>=5` by
  `no_eq_four_of_near_diagonal_ge_five`, so the combined theorem
  `no_lower_block_prime_of_eq_four_ge_five` proves that every remaining
  `N=4,k>=5` solution must have no prime in `[n+1,n+k]`. Bertrand is now
  packaged as `exists_lower_block_prime_of_n_le_k`, and
  `eq_four_ge_five_forces_k_lt_n` proves that every remaining separated
  `N=4,k>=5` solution must satisfy `k<n`. The near-diagonal contrapositive is
  now named as `eq_four_ge_five_forces_d_add_k_lt_n`, proving every remaining
  separated `N=4,k>=5` solution must satisfy `d+k<n`. Together with `k<=d`,
  this gives the sparse-window corollary
  `eq_four_ge_five_forces_two_mul_k_lt_n`, proving `2*k<n`. The far-shift
  branch is also Lean-closed: `eq_four_ge_five_forces_d_lt_n_add_k` proves that every
  remaining separated `N=4,k>=5` solution must satisfy `d<n+k`. The bundled
  theorem `eq_four_ge_five_remaining_window` packages the current all-`k`
  reduction: from `P_k(n+d)=4P_k(n)`, `k<=d`, and `k>=5`, any remaining row has
  `k<n`, `2*k<n`, `d+k<n`, `d<n+k`, `d | 3*P_k(n)`, and no prime in
  `[n+1,n+k]`.
  The module also proves the square-product and valuation package:
  `intervalProd_mul_square_of_eq_four`,
  `intervalProd_factorization_eq_of_odd_prime`, and
  `intervalProd_factorization_two_eq_add_two`.
  New Lean smoothness filter: `no_eq_four_of_lower_prime_factor_gt_shift_tail`
  and `lower_prime_factor_le_shift_tail_of_eq_four` prove that, in any
  separated `N=4` interval equation, every prime divisor `p` of each lower
  factor `n+r` satisfies `p<=d-r+k`. This strengthens the lower-block-prime
  obstruction into a per-factor smoothness constraint.
  In the remaining `k>=5` window,
  `lower_factor_dvd_smaller_intervalProd_of_eq_four_ge_five` and
  `lower_prime_factor_lt_lower_factor_of_eq_four_ge_five` additionally prove
  that each `n+r` divides a product whose final factor is strictly below
  `n+r`, and every prime divisor of `n+r` is strictly below `n+r`.
  New exact prefix checker `scripts/verify_erdos686_k3_n4_prefix.py` verifies
  no admissible `N=4,k=3` solution for shifted variable `2<=Y<=20000000`,
  where `X=m+2`, `Y=n+2`, in the cubic equation `X^3-X=4(Y^3-Y)`; artifact:
  `examples/erdos-problems/attack/erdos686-k3-n4-prefix-20m-draft.v1.json`.
  New focused verifier `scripts/verify_erdos686_n4_k3_k6_k8.py` banks the GPT
  Pro follow-up as a conditional/local certificate: for `k=3`, assuming the
  quoted irrationality measure
  `|cuberoot(4)-v/u| > 0.072*u^(-2.45)`, it reduces to the exact finite check
  `1<=u<58000`, `C|60`, `v^3=4u^3-C`, `C*D^2=4u-v`, with no admissible row.
  The same verifier also checks elementary `N=4` exclusions for `k=6` and
  `k=8` from polynomial-square identities plus finite boxes. The full local
  `k=6` and `k=8` rows are now Lean-kernelized: `k6_square_identity`,
  `k6SquareApprox_odd`,
  `k6SquareResidual_lt_784_mul_sq_of_ge_200`,
  `k6SquareResidual_pos`, `k6SquareResidual_lt_approx_of_ge_200`,
  `two_mul_k6SquareResidual_lt_approx_of_ge_200`,
  `k6_scaled_intervalProd_lt_approx_sq`,
  `k6_approx_sub_one_sq_lt_scaled_intervalProd_of_ge_200`,
  `k6_two_approx_sub_one_sq_lt_four_scaled_intervalProd_of_ge_200`,
  `k6_product_eq_four_impossible_of_ge_200`, `k6_finite_box_cutoff`,
  `k6_finite_box_no_hits`, `k6_small_n_large_m_impossible`, `no_k6_four`,
  `k8_square_identity`, `k8SquareApprox_even`,
  `k8SquareResidual_lt_100_mul_sq_of_ge_20`, `k8SquareResidual_pos`,
  `k8SquareResidual_lt_approx_of_ge_20`, `odd_sq_ne_four_mul`,
  `k8SquareResidual_ne_four_mul`,
  `k8_product_eq_four_impossible_of_approx_eq`,
  `k8_approx_eq_of_product_eq_of_ge_20`,
  `k8_product_eq_four_impossible_of_ge_20`, `k8_finite_box_cutoff`,
  `k8_finite_box_no_hits`, `k8_small_n_large_m_impossible`, and `no_k8_four`.
  The full local `N=4,k=6` and `N=4,k=8` rows are now Lean-kernelized: there are
  no admissible `m,n` with `n+6<=m` and `P_6(m)=4P_6(n)`, nor with `n+8<=m`
  and `P_8(m)=4P_8(n)`. Draft artifact:
  `examples/erdos-problems/attack/erdos686-n4-k3-k6-k8-gptpro-certificate-draft.v1.json`.
  The verifier now has `--verify` mode for replaying that artifact.
  New method-profile verifier `scripts/verify_erdos686_even_square_method.py`
  computes the centered even-`k` identity
  `F_r(y)=prod_j(y^2-(2j-1)^2)=2^(2r)P_{2r}(x)`, the formal square-root part
  `H_r`, and residual `R_r=H_r^2-F_r`. Through `r<=80` (`k<=160`), odd `r`
  rows have the dyadic lattice obstruction and even `r` rows have a modular
  obstruction after the forced equations `H_r(Y_m)=2H_r(Y_n)` and
  `R_r(Y_m)=4R_r(Y_n)`. Artifact:
  `examples/erdos-problems/attack/erdos686-even-square-method-r80-draft.v1.json`.
  The script now records a sharper odd-`r` denominator profile: for `r=2s+1`,
  checked rows satisfy `denom_lcm(H_r)=2^(s+v2(s!))`, and after clearing this
  denominator the linear coefficient is odd while every other nonzero
  coefficient is even. This gives the proof-shaped uniform lemma that
  `denom_lcm(H_r)*H_r(y)` is odd for every odd `y`. The script's replay mode
  recomputes all `80` rows and compares top-level summaries, polynomial
  summaries, dyadic lattice profiles, denominator-pattern fields, and modular
  obstruction fields. Current replay has `errors: []`.
  It also records the obstruction to the naive odd-`r` tail route: the crude
  sufficient inequality `D*(R_r(y)+4*R_r(y)) < H_r(y)` has asymptotic crossover
  near `5*D*lead(R_r)`, already `405625` at `r=5` and `1648559185/4` at `r=7`.
  A useful proof needs an equality-compatible tail bound, not just a raw
  denominator-gap finite check.
  This is a bounded profile for a possible certificate generator, not an
  all-even proof; it also kills the tempting shortcut "modulus <=100 always
  suffices", first failing at `r=16`, `k=32`, with first modulus `121`.
  Only `k=2,4,6,8` are fully Lean-kernelized. `k=3` still depends on an
  external irrationality estimate. The full `k=6` and `k=8` rows are now
  Lean-kernelized as `no_k6_four` and `no_k8_four`.
  New focused verifier `scripts/verify_erdos686_k10_n4.py` gives an exact
  script-backed local certificate for `N=4,k=10`. It uses the centered
  `r=5`, `y=2*x+11` identity `F_5(y)=2^10*P_10(x)=H(y)^2-R(y)`, with
  `H(y)=y^5-(165/2)y^3+(7887/8)y` and `D=8`. It symbolically checks
  `D*H(odd)` is odd, `R(y)>0` for `y>=11`, and the tail inequality
  `4*D^2*R(y)<4*D*H(y)-1` for every `y>=81127` by shifting the tail polynomial
  at `81127` and checking nonnegative coefficients with positive constant term.
  The remaining exact finite box is `0<=n<40558`, with one binary search per
  `n` since `P_10(m)` is strictly increasing in `m`; it finds `0` hits and a
  global finite-box cutoff `m<=46588`. Draft artifact:
  `examples/erdos-problems/attack/erdos686-n4-k10-certificate-draft.v1.json`.
  New focused verifier `scripts/verify_erdos686_k12_n4.py` gives an exact
  script-backed local certificate for `N=4,k=12`. It uses the centered
  `r=6`, `y=2*x+13` identity `F_6(y)=2^12*P_12(x)=H(y)^2-R(y)`, with
  `H(y)=y^6-143*y^4+4147*y^2-24453` and
  `R(y)=2223936*y^4-73996416*y^2+489893184`. It proves the tail from
  `y>=1493` (`n>=740`) by shifted coefficient positivity, then forces
  `H(Y_m)=2*H(Y_n)` and uses a modulo `13` residual obstruction. The remaining
  exact finite box is `0<=n<740`, `m>=n+12`; it finds `0` hits and a global
  finite-box cutoff `m<=830`. Draft artifact:
  `examples/erdos-problems/attack/erdos686-n4-k12-certificate-draft.v1.json`.
  New focused verifier `scripts/verify_erdos686_k14_n4.py` gives an exact
  script-backed local certificate for `N=4,k=14`. It uses the centered
  `r=7`, `y=2*x+15` identity `F_7(y)=2^14*P_14(x)=H(y)^2-R(y)`, clears
  denominators with `D=16`, proves `D*H(odd)` is odd, and proves the tail from
  `y>=82427960` (`n>=41213973`) by shifted coefficient positivity. The tail
  forces `H(Y_m)=2*H(Y_n)`, contradicting the odd/even parity after multiplying
  by `D`. The remaining exact finite box is `0<=n<41213973`, `m>=n+14`; an
  exact recurrence over the minimal admissible `m` finds `0` hits and largest
  minimal candidate `m=45503916`. Draft artifact:
  `examples/erdos-problems/attack/erdos686-n4-k14-certificate-draft.v1.json`.
  New focused verifier `scripts/verify_erdos686_k16_n4.py` gives an exact
  script-backed local certificate for `N=4,k=16`. It uses the centered
  `r=8`, `y=2*x+17` identity `F_8(y)=2^16*P_16(x)=H(y)^2-R(y)`, proves the
  tail from `y>=46251` (`n>=23117`) by shifted coefficient positivity, then
  forces `H(Y_m)=2*H(Y_n)` and uses a modulo `17` residual obstruction. The
  remaining exact finite box is `0<=n<23117`, `m>=n+16`; it finds `0` hits and
  a global finite-box cutoff `m<=25208`. Draft artifact:
  `examples/erdos-problems/attack/erdos686-n4-k16-certificate-draft.v1.json`.
  New focused verifier `scripts/verify_erdos686_k20_n4.py` gives an exact
  script-backed local certificate for `N=4,k=20`. It uses the centered
  `r=10`, `y=2*x+21` identity `F_10(y)=2^20*P_20(x)=H(y)^2-R(y)`, proves the
  tail from `y>=1796958` (`n>=898469`) by shifted coefficient positivity, then
  forces `H(Y_m)=2*H(Y_n)` and uses a modulo `9` residual obstruction. The
  remaining exact finite box is `0<=n<898469`, `m>=n+20`; exact recurrence over
  the minimal admissible `m` finds `0` hits and largest minimal candidate
  `m=962955`. Draft artifact:
  `examples/erdos-problems/attack/erdos686-n4-k20-certificate-draft.v1.json`.
  New focused verifier `scripts/verify_erdos686_k24_n4.py` gives an exact
  script-backed local certificate for `N=4,k=24`. It uses the centered
  `r=12`, `y=2*x+25` identity `F_12(y)=2^24*P_24(x)=H(y)^2-R(y)`, proves the
  tail from `y>=84007164` (`n>=42003570`) by shifted coefficient positivity,
  then forces `H(Y_m)=2*H(Y_n)` and uses a modulo `25` residual obstruction. The
  remaining exact finite box is `0<=n<42003570`, `m>=n+24`; exact recurrence
  over the minimal admissible `m` finds `0` hits and largest minimal candidate
  `m=44501232`. Draft artifact:
  `examples/erdos-problems/attack/erdos686-n4-k24-certificate-draft.v1.json`.
  The first remaining odd-`r` row `k=18` has the algebraic dyadic obstruction,
  but the direct tail starts only at `Y=612477216640` (`n>=306238608311`), so
  the current one-row finite-box route is not practical there without a sharper
  equality-compatible tail bound.
  Focused follow-up prompt for the remaining all-`k` obstruction:
  `docs/erdos-attack/686/gpt-pro-n4-all-k-obstruction-prompt.md`. The older
  `docs/erdos-attack/686/gpt-pro-k3-n4-integral-points-prompt.md` is now mostly
  superseded by the conditional `k=3` certificate above.
  This is theorem-grade partial progress only; the missing input is still an
  all-`k` obstruction or a reduction from the remaining higher `k` to the
  `k=2` case.
- #312: plausible structural route, not a local close. The missing input is an
  exponential-density theorem for bounded-denominator reciprocal subset sums,
  equivalently a height-sensitive zero-sum theorem in `Z/lcm(1,...,D)Z`. New
  Lean module `lean/Vela/Erdos/Erdos312.lean` now banks the large-denominator greedy
  branch: `greedy_weight_prefix` proves that a finite rational weighted list
  with total mass above `1` and every term at most `eps` has a prefix in
  `(1-eps,1]`, and `greedy_reciprocal_prefix` specializes this to denominator
  lists all at least `M>0`, producing a prefix reciprocal sum in `(1-1/M,1]`.
  The same module now banks the repeated-denominator exact branch:
  `replicate_reciprocalWeight_sum_eq_one` proves that `d` copies of denominator
  `d>0` sum to `1`, and
  `exists_reciprocal_submultiset_sum_one_of_le_count` proves that any
  denominator multiset containing at least `d` copies of `d` contains an exact
  reciprocal submultiset summing to `1`.
  The same module also proves the fixed-cutoff residual budget:
  `bounded_denominator_card_triangular_of_count_lt_self` says that if all
  remaining denominators lie in `1..M` and every denominator `d` appears fewer
  than `d` times, then `2*card <= M*(M-1)`. The new weighted count lemmas
  `reciprocalWeight_sum_eq_sum_count_nsmul_of_mem_Icc` and
  `bounded_denominator_reciprocal_sum_le_of_count_lt_self` also give the crude
  residual mass ceiling `sum 1/n <= M`. The wrappers
  `bounded_denominator_card_triangular_of_no_reciprocal_submultiset_sum_one`
  and
  `bounded_denominator_reciprocal_sum_le_of_no_reciprocal_submultiset_sum_one`
  now derive these bounds directly from the absence of an exact reciprocal
  submultiset summing to `1`.
  The latest Lean pass packages the split as
  `cutoff_exact_or_large_tail_greedy_or_total_mass_le`: for any positive
  denominator multiset and cutoff `M`, either there is an exact submultiset
  summing to `1`, or the tail above `M` contains a greedy near-hit in
  `(1 - 1/(M+1), 1]`, or the total reciprocal mass is at most `M+1`.
  The natural-parameter corollary
  `exists_reciprocal_submultiset_near_one_of_nat_lt_sum` now proves that if
  `K>1`, all denominators are positive, and total reciprocal mass is greater
  than `K`, then some submultiset has mass in `(1 - 1/K, 1]`.
  The residual estimate has also been sharpened by the harmonic complement:
  `harmonicReciprocalPrefix`,
  `sum_Icc_pred_nsmul_reciprocalWeight_add_harmonic_eq`,
  `bounded_denominator_reciprocal_sum_add_harmonic_le_of_count_lt_self`,
  `bounded_denominator_reciprocal_sum_add_harmonic_le_of_no_reciprocal_submultiset_sum_one`,
  and
  `cutoff_total_reciprocal_sum_add_harmonic_le_of_no_exact_of_large_tail_le_one`
  prove that, in the no-exact and small-tail branch,
  `H_M + total_mass <= M+1`. The direct cutoff theorem
  `exists_reciprocal_submultiset_near_one_of_cutoff_lt_sum_add_harmonic` proves
  that `H_M + total_mass > M+1` already gives a submultiset with mass in
  `(1 - 1/(M+1), 1]`. The equivalent deficit-form theorem
  `exists_reciprocal_submultiset_near_one_of_cutoff_deficit_lt_sum` uses the
  future-proof-facing hypothesis `total_mass > M+1-H_M`. The wrapper
  `exists_reciprocal_submultiset_near_one_of_cutoff_deficit_lt_threshold`
  accepts an external rational lower bound `K < total_mass` plus
  `M+1-H_M < K`, which is the clean Lean interface for any future cutoff-
  selection argument. The corollary
  `exists_reciprocal_submultiset_near_one_of_nat_lt_sum_add_harmonic` improves
  the natural trigger to `H_{K-1} + total_mass > K` while retaining the same
  near-one gap `(1 - 1/K, 1]`.
  This is only a weak polynomial baseline: it is below the known Erdős-Graham
  `K^-2` scale and does not approach the requested exponential gap.
  This is a real proof branch, but #312 still needs the bounded/medium-
  denominator entropy or exactness theorem.
- #124: Lean-green interval-block reduction, not a local close. New file
  `lean/Vela/Erdos/Erdos124IntervalBlock.lean` proves the elementary syndetic-plus-
  interval criterion, and `scripts/verify_124_begl_framework.py` reproduces the
  BEGL `{3,4,7}`, `k=1` largest non-representable integer `581`. The remaining
  open core is the global interval-block existence lemma for every admissible
  `D,k`.
- #463: Lean-backed exact reductions are strong, including shifted-sieve and
  semiprime-gap bridges, but the residual is a uniform rough-composite
  existence theorem in short intervals. Almost-all published rough-number
  inputs do not meet the all-large-`n` quantifier.
- #319: Lean-backed smoothness and anchor mechanisms are useful, and exact
  tables are verified only at small `N`. The official problem asks for the
  asymptotic size, and the current bounds still leave the constant gap between
  the Croot/Adenwalla lower construction and the smoothness upper barrier.
- #684: partial effective lower-bound progress, not a local close. New verifier
  `scripts/verify_erdos684_subsequence_lower_bound.py` checks the construction
  `M_K=prod_{p<=K} p^(floor(log_p K)+1)`, `n_K=M_K-1`, proving by Kummer
  carry-counting that `f(n_K)>K` for every checked `3<=K<=40`. The theorem
  shape is general and gives the subsequential lower-bound form
  `f(n_K)>=(1/2+o(1))log n_K`; it does not give an upper bound or an all-`n`
  lower bound. Draft artifact:
  `examples/erdos-problems/attack/erdos684-subsequence-lower-bound-draft.v1.json`.
- #848: nearby finite-check lead. The live page marks it `DECIDABLE`, and
  Sawhney's linked proof plus the external Lean file prove only the asymptotic
  all-sufficiently-large-`N` theorem, not the original all-`N` statement. The
  forum now links Nat Sothanaphan's v5 explicit-threshold note claiming
  `N >= 2.64e17`; new verifier
  `scripts/verify_erdos848_explicit_threshold_v5_numerics.py` audits the
  numeric constants and final case margins in that note. It checks `11`
  inequalities, including `E25(2.64e17)<4.700`, `E100(2.64e17)<5.275`, the
  Corollary-2 density errors, and all four final density cases below `0.04`;
  replay returns `errors: []`. Draft artifact:
  `examples/erdos-problems/attack/erdos848-explicit-threshold-v5-numerics-draft.v1.json`.
  This is not a proof of the explicit threshold, since the v5 note itself still
  needs independent proof review/formalization. New exact verifier
  `scripts/verify_erdos848_prefix_clique.py` found no
  counterexample for every `N <= 2000`, with selected exact checks at
  `N=2500,3000,4000,5000,7500,10000,15000,20000` also matching the
  residue-7 class. New endpoint verifier
  `scripts/verify_erdos848_endpoint_reduction.py` uses monotonicity of clique
  number and constancy of the residue-7 target between jumps to prove no
  counterexample for every `N <= 20000` from `801` endpoint checks. It now has
  replay mode; `--verify` recomputes the artifact from its saved `max_N` and
  compares endpoint rows, witnesses, clique optima, search nodes, and summary
  fields. Current replay returns `errors: []`. Draft
  artifacts: `examples/erdos-problems/attack/erdos848-prefix-clique-draft.v1.json`
  and
  `examples/erdos-problems/attack/erdos848-endpoint-reduction-20000-draft.v1.json`.
  New obstruction verifier
  `scripts/verify_erdos848_block_coloring_obstruction.py` kills the naive
  global coloring shortcut by the smallest block counterexample `(32,43)`;
  artifact:
  `examples/erdos-problems/attack/erdos848-block-coloring-obstruction-draft.v1.json`.
  New square-class count verifier
  `scripts/verify_erdos848_square_class_count_certificate.py` compiles
  `scripts/erdos848_square_class_count_certificate.cpp` and verifies the
  finite certificate requested by the `N=10^10` Sawhney-style threshold audit:
  for every `b <= 10^10`,
  `R(b) * #{m <= floor((b*10^10+1)/19200000^2) : m^{-1} is square mod b}
  <= 813802`. It checks `38` kernel cases with maximum terminal value
  `813792`, so the certificate has slack `10`. Draft artifact:
  `examples/erdos-problems/attack/erdos848-square-class-count-certificate-draft.v1.json`.
  This verifies the finite count certificate only; it does not formalize the
  analytic skeleton or cover the remaining finite range below `10^10`.
  Direct inspection of the external Lean file shows only an existential
  eventual threshold, not a concrete computable `N0`. This is not a solve; even
  accepting the v5 threshold would leave the interval `N < 2.64e17` uncovered
  by the current repo-verified finite check.
- #835: finite-witness target, but not a quick close. New verifier
  `scripts/verify_erdos835_small_cases.py` proves exact no-colouring for
  `k=3` and `k=4`, and records the standard design reduction: any solution
  gives a large set of `k+1` Steiner systems `S(k-1,k,2k)`. New verifier
  `scripts/verify_erdos835_k6_witt.py` proves the first prime case `k=6`
  is impossible by enumerating all `5040` labelled `S(5,6,12)` designs and
  checking that their disjointness graph is triangle-free. The live page
  already attributes the composite `k+1` exclusion to Ma and Tang, so the
  remaining full-solve target is the prime case beyond the known derived
  rows. New verifier `scripts/verify_erdos835_alltop_parameters.py` checks the
  parameter arithmetic behind the Alltop shift
  `LS[p](p-2,p-1,2p-2) iff LS[p](p-3,p-2,2p-3)`, records that `p=11,13` fall
  to known `S(4,5,15)` and `S(4,5,17)` nonexistence, and explains why fixed
  low-depth derived nonexistence cannot close all primes once paired with
  Keevash-type large-set existence. The current clean target is: prove no
  large set of `p` systems `S(p-3,p-2,2p-3)` for every prime `p>=17`, or find
  one explicit colouring. Draft artifacts:
  `examples/erdos-problems/attack/erdos835-small-cases-draft.v1.json` and
  `examples/erdos-problems/attack/erdos835-k6-witt-exclusion-draft.v1.json`,
  plus `examples/erdos-problems/attack/erdos835-alltop-reduction-draft.v1.json`.
  This is not a solve.
- #850: finite-positive-witness target, still open and not a quick close.
  A single pair `x != y` with equal prime-factor supports for `x,x+1,x+2` and
  `y,y+1,y+2` would solve it positively. New exact sorted-signature search
  `scripts/erdos850_radical_triple_search.cpp` found no duplicate triple
  `(rad(n),rad(n+1),rad(n+2))` for `1 <= n <= 200000000`. Draft artifact:
  `examples/erdos-problems/attack/erdos850-radical-triples-prefix200m-draft.v1.json`.
  This is bounded evidence only and is not a solve.
- #128: finite-counterexample target, but the natural `C5` blow-up route is
  now blocked at the exact continuous threshold. New nauty verifier
  `scripts/verify_erdos128_geng_exclusion.py` proves no counterexample for
  `n=12,13` by checking `13,751,645` non-isomorphic triangle-free graphs after
  the necessary edge lower bound; draft artifact:
  `examples/erdos-problems/attack/erdos128-geng-exclusion-n12-13-draft.v1.json`.
  New verifier
  `scripts/verify_erdos128_c5_blowup_bound.py` proves that every weighted
  `C5` blow-up has a fractional half-subgraph with edge density at most
  `1/50`; balanced `C5` attains equality. Draft artifact:
  `examples/erdos-problems/attack/erdos128-c5-blowup-bound-draft.v1.json`.
  This rules out only the asymptotic weighted-`C5` route and is not a solve.

## Ranked target table

| Rank | Target | Status | Proposed grade | What to bank |
| --- | --- | --- | --- | --- |
| 1 | #302 | PARTIAL | `improved_published_bound` + `partial_proof` | Bank `examples/erdos-problems/attack/erdos302-base241920-prefix139-scip-monotone-bound-record-draft.v1.json` as the lead numerical draft. Also review `examples/erdos-problems/attack/erdos302-dyadic-limit-record-draft.v1.json`, which pins limit existence for the fixed dyadic family `945*2^a` only. |
| 2 | #267 | PARTIAL | `partial_proof` | Bank `examples/erdos-problems/attack/erdos267-badea-ge-two-lean-bridge-record-draft.v1.json` only for the known `c >= 2` territory. Exact citation: Badea 1993 Corollary 3.2 for this application; Badea 1987 is background. |
| 3 | #463 | PARTIAL | `obstruction_map` | Bank `examples/erdos-problems/attack/erdos463-margin-prefix-obstruction-record-draft.v1.json` as Lean-backed finite-prefix evidence only. Artifact: `examples/erdos-problems/attack/erdos463-margin-prefix-10m-draft.v1.json`. |
| 4 | #319 | PARTIAL | `partial_proof` | Bank `examples/erdos-problems/attack/erdos319-mitm-exact-table-record-draft.v1.json` as an exact finite table through `N=27`. Do not bank a solve from the finite table or the Theta observation. |
| 5 | #417 | PARTIAL | `partial_proof` | Bank `examples/erdos-problems/attack/erdos417-hidden-totient-decomposition-record-draft.v1.json` as a Lean-backed reduction of the gap to hidden first-preimage totients. It does not prove limit existence or strict excess. |

## #302 result

Claim for review:

`Erdos #302: the checkpointed layered integer-prefix method at base 241920
certifies f(N) <= (140981/163520) N + o(N) using checkpoint prefixes
through 130, SCIP-certified prefix decisions through 139, and monotone tail
lower bounds; this improves the prefix-138 bound but is not a full solution.`

Exact data:

- Base: `241920 = 2^8 * 3^3 * 5 * 7`
- Divisors excluding `1`: `143`
- Within-divisor triples: `942`
- Dilation density: `18/73`
- Certified prefix count: `139`
- Checkpoint completed prefix count: `130`
- Monotone tail prefixes: `4`
- Tail lower bound: `70`
- Weighted prefix-cover deficit lower bound: `7513/13440`
- Forced deletion lower bound: `22539/163520`
- Upper bound: `140981/163520 = 0.862163649706458`
- Superseded checkpoint-tail bound: `845891/981120 = 0.8621687459230267` at base `241920`, cutoff `138`
- Superseded checkpoint-tail bound: `140983/163520 = 0.8621758806262231` at base `241920`, cutoff `135`
- Superseded checkpoint-tail bound: `281969/327040 = 0.8621850538160469` at base `241920`, cutoff `132`
- Superseded checkpoint-tail bound: `845921/981120 = 0.8621993232224396` at base `241920`, cutoff `131`
- Superseded checkpoint-tail bound: `7553/8760 = 0.8622146118721461` at base `241920`, cutoff `130`
- Superseded checkpoint-tail bound: `84611/98112 = 0.862391960208741` at base `241920`, cutoff `118`
- Superseded thread bound: `211121/244800 = 0.862422385620915` at base `120960`
- Superseded thread bound: `13151/15240 = 0.8629265091863517` at base `60480`
- Superseded thread bound: `5807/6720 = 0.8641369047619047` at base `30240`
- Previous tracked bound: `155923/180048 = 0.8660079534346397` at base `45360`
- Published page baseline: van Doorn `9/10 + o(1)` and Cambie lower bound `5/8 + o(1)` are recorded on Erdős Problems #302.

Files:

- `examples/erdos-problems/attack/erdos302-base241920-prefix139-scip-monotone-bound-draft.v1.json`
- `examples/erdos-problems/attack/erdos302-base241920-prefix139-scip-monotone-bound-verify.v1.json`
- `examples/erdos-problems/attack/erdos302-base241920-prefix139-scip-monotone-bound-record-draft.v1.json`
- `examples/erdos-problems/attack/erdos302-base241920.checkpoint.json`
- `scripts/erdos302_prefix_scip_decision.py`
- `scripts/erdos302_checkpoint_bound.py`
- `scripts/erdos302_layered_jump_build.py`
- `scripts/erdos302_layered_jump_verify.py`
- Superseded but still valid exact-full-prefix row: `examples/erdos-problems/attack/erdos302-base120960-layered-prefix-bound-draft.v1.json`
- Superseded but still valid exact-full-prefix record: `examples/erdos-problems/attack/erdos302-base120960-layered-prefix-bound-record-draft.v1.json`
- Superseded but still valid: `examples/erdos-problems/attack/erdos302-base60480-layered-prefix-bound-draft.v1.json`
- Superseded but still valid: `examples/erdos-problems/attack/erdos302-base60480-layered-prefix-bound-record-draft.v1.json`
- Superseded but still valid: `examples/erdos-problems/attack/erdos302-base30240-layered-prefix-bound-draft.v1.json`
- Superseded but still valid: `examples/erdos-problems/attack/erdos302-base30240-layered-prefix-bound-record-draft.v1.json`

Checkpoint builder:

```bash
gcloud compute ssh vela-compute-1 --zone us-east4-a --command='cd ~/erdos302 && timeout 10800 python3 erdos302_layered_jump_build.py 241920 --seconds 1800 --workers 16 --checkpoint erdos302-base241920.checkpoint.json --out erdos302-base241920-layered-prefix-bound-draft.v1.json'
```

Builder result:

```text
Checkpoint reached prefix 130/143 with r=65. Prefix 131 returned UNKNOWN after
1800.057s, so no full exact-prefix artifact was claimed for base 241920.
```

Independent recheck:

```bash
python3 scripts/erdos302_prefix_scip_decision.py examples/erdos-problems/attack/erdos302-base241920.checkpoint.json --prefix-size 139 --prior-r 66 --prior-r 67 --prior-r 67 --prior-r 67 --prior-r 68 --prior-r 68 --prior-r 69 --prior-r 69 --seconds 1200 --out examples/erdos-problems/attack/erdos302-base241920-prefix139-scip-monotone-bound-draft.v1.json > examples/erdos-problems/attack/erdos302-base241920-prefix139-scip-monotone-bound-verify.v1.json
```

Key output:

```text
base=241920, divisor_count=143, triple_count=942
checkpoint_prefix_count=130, prior_r=[66,67,67,67,68,68,69,69], prefix_cover_number=70, prefix_independence_number=69
certified_prefix_count=139, tail_prefix_count=4, tail_lower_bound=70
delta=18/73, weighted_deficit=7513/13440, bound=140981/163520
errors=[]
```

Fresh local replay note:

A later local one-second smoke replay of the `241920` checkpoint verified the
arithmetic and easy early jumps but intentionally returned UNKNOWN on hard later
jumps. Do not count the smoke run as verifier evidence. The SCIP prefix-139 decision replay
above is the verifier evidence.

Trust gate:

- `deliverable_grade`: `improved_published_bound`
- `grade_gate`: `ok`
- `gate_status`: `verified`
- Attachments: `vva_230e4bf025dd5e13`, `vva_7e85aebb2b048974`

Additional local sanity check:

```bash
python3 scripts/verify_erdos302_bound.py
```

Output reproduced both published baselines, the tracked `45360` row, and the new
`30240` row exactly, ending with `RESULT: ALL VERIFIED`.

Caveat:

This is not a full solution of #302. It is a stronger valid layered prefix-cover
lower-bound certificate. Unlike the `120960` row, the `241920` row does not
claim exact optimum cover numbers through all prefixes; prefixes after `139` are
handled only by monotonicity. The aggregate LP relaxation remains excluded as a
theorem.

Dyadic-family limit wind-down:

- Proof note: `docs/erdos-attack/302/DYADIC_LIMIT.md`.
- Draft record:
  `examples/erdos-problems/attack/erdos302-dyadic-limit-record-draft.v1.json`.
- Re-check script: `scripts/verify_erdos302_dyadic_limit.py`.
- Exact family: `n_a = 945*2^a = 2^a * 3^3 * 5 * 7`.
- Density factor:
  `delta_a = (63/128) * 2^a/(2^(a+1)-1) -> 63/256`.
- Infinite divisor set:
  `D_infty = {2^i h : i >= 0, h | 945} \ {1}`.
- Result: for the exact full-row constants
  `C_a = 1 - delta_a * W_a`, the fixed-family limit exists and is
  `C_infty = 1 - (63/256) * W_infty`, where `W_infty` is the convergent
  infinite prefix-cover series over `D_infty`.
- Verification:
  `python3 scripts/verify_erdos302_dyadic_limit.py examples/erdos-problems/attack/erdos302-dyadic-limit-record-draft.v1.json`
  returns `errors=[]`.

Scope caveat:

This pins the asymptotic expression for the dyadic construction family only. It
does not compute the numerical value of `C_infty`, does not prove a global
infimum over layered bases/cutoffs, and does not answer #302. The lead numerical
draft remains `140981/163520 = 0.862163649706458`.

Refocus note:

Per the latest instruction, stop #302 here. Do not launch more base sweeps or
base-prefix grind jobs.

Tail-ceiling check:

- Draft record:
  `examples/erdos-problems/attack/erdos302-base241920-tail-ceiling-record-draft.v1.json`.
- Independent verifier: `scripts/verify_erdos302_tail_ceiling.py`.
- Exact result: the four remaining divisor prefixes at base `241920` can improve
  the row by at most `1/98112`.
- Therefore the exact full-base-241920 layered constant, if those four prefixes
  were closed, lies in
  `[211469/245280, 140981/163520]`.
- This is only a tail ceiling for the already banked base. It is not a new
  upper-bound improvement and not an asymptotic-limit proof.

Search notes:

- `120960`, `60480`, and `30240` are valid and gate-verified but superseded by
  the `241920` prefix-139 SCIP monotone-tail bound.
- `90720 = 2^5 * 3^4 * 5 * 7` finished with the jump builder on
  `vela-compute-1`: divisor count `119`, triples `724`, delta `30/121`,
  weighted deficit `835/1512`, forced deletion `4175/30492`, and bound
  `26317/30492 = 0.8630788403515677`. This is exact but weaker than `60480`,
  so it is not the lead bank record. Artifact:
  `examples/erdos-problems/attack/erdos302-base90720-layered-prefix-bound-draft.v1.json`.
- `181440 = 2^6 * 3^4 * 5 * 7` was attempted with the jump builder on
  `vela-compute-1`. It reached prefix `120/139` with `r=57` before a late
  prefix proof ran too long for this pass. No artifact was produced and no bound
  is claimed.
- `241920 = 2^8 * 3^3 * 5 * 7` did not finish as a full exact-prefix artifact:
  the checkpoint reached `130/143` with `r=65`, then prefix `131` returned
  UNKNOWN after `1800.057s`. The banked claim uses only the independently
  verified cutoff `139/143` plus monotone tail lower bounds.
- Prefix `140` at the same base was attempted after the prefix-139 result but
  stopped with no verifier JSON and no artifact. Do not claim anything from it.
- The exact jump verifier is faster than objective minimization: it checks cover
  witnesses and proves only the cover-number jumps by UNSAT decisions. Use it for
  nearby `2^a 3^b 5 7` bases. Treat unfinished rows as evidence only.
- `scripts/erdos302_layered_jump_build.py` supports `--checkpoint`, `--resume`,
  and `--stop-after-prefix` for long VM runs. A local stop/resume test at base
  `30240` reproduced `5807/6720`; the resumed artifact independently verified
  with `errors=[]`.

## #307 status

Dedicated #307 thread status: partial reduction only. No nonexistence proof or
finite reduction has met the solve bar, and the 2026-06-07 residual-port
assessment did not produce a plausible global-bound lemma for `R`, `|Q|`, or
admissible cores `S`.

New Lean support in `lean/Vela/Erdos/Erdos307.lean`:

- `disjoint_of_primeSet_left_derivative_eq_product`: if `P` is a finite prime
  set and `A(P)=M(Q)`, then `P ∩ Q = ∅`. Shared primes are excluded by the
  derivative-product equation itself, so disjointness is not an independent
  search assumption once the cycle equation is present.
- `twoCycle_of_primeSet_cycle_equations`: finite prime sets satisfying
  `A(P)=M(Q)` and `A(Q)=M(P)` assemble directly into the existing `TwoCycle`
  structure.
- `A_union_of_disjoint`, `twoCycle_union_derivative_sum`, and
  `twoCycle_union_discriminant_square`: any actual squarefree derivative
  2-cycle forces the union identities `M(P ∪ Q)=M(P)M(Q)` and
  `A(P ∪ Q)=M(P)^2+M(Q)^2`, hence the fixed-union discriminant-square
  obstruction for the real union `P ∪ Q`.
- `twoCycle_left_mem_dvd_right_not_left_A` and
  `twoCycle_right_mem_dvd_left_not_right_A`: every prime on one side divides
  the opposite derivative numerator but not its own side's derivative numerator.
  This is the Lean-level form of the local modular obstruction used by
  partition filters.
- `twoCycle_products_coprime`,
  `twoCycle_left_product_dvd_right_coprime_left_A`, and
  `twoCycle_right_product_dvd_left_coprime_right_A`: the side products are
  coprime, and each side product divides the opposite derivative numerator
  while remaining coprime to its own derivative numerator.
- `twoCycle_left_card_ge_two` and `twoCycle_right_card_ge_two`: each side of a
  squarefree derivative 2-cycle has at least two primes. This is only a tiny
  formal lower bound; it does not approach the known mass lower bound.
- `twoCycle_union_card_ge_four`: the union of the two sides has at least four
  primes, derived from the side lower bounds and disjointness. This is likewise
  a small formal scaffold, not the known `|P ∪ Q| >= 60` mass bound.
- `A_mod_mem_eq_M_div_self`,
  `twoCycle_left_mem_union_div_eq_square_mod`, and
  `twoCycle_right_mem_union_div_eq_square_mod`, with union wrapper
  `twoCycle_mem_union_div_is_square_mod`: for every prime in the union, the
  diagonal product `M(P ∪ Q)/p` is congruent to a square modulo `p`. This is the
  Lean-level statement behind the union-first internal square-residue filter.
- `M_mod_two_of_mem_two`, `M_mod_two_of_not_mem_two`,
  `A_mod_two_of_mem_two`, `A_mod_two_of_not_mem_two`,
  `twoCycle_not_two_mem_both`,
  `twoCycle_left_card_odd_of_two_not_mem`,
  `twoCycle_right_card_odd_of_two_not_mem`,
  `twoCycle_right_card_even_of_two_mem_left`, and
  `twoCycle_left_card_even_of_two_mem_right`: the exact parity law is now
  Lean-proven. If neither side contains `2`, both side cardinalities are odd; if
  one side contains `2`, the opposite side cardinality is even. This is still a
  necessary screen only, not a nonexistence proof.
- `odd_square_mod_eight`, `two_mod_four_square_mod_eight`,
  `M_square_mod_eight_of_mem_two`,
  `M_square_mod_eight_of_not_mem_two`, and `twoCycle_union_A_mod_eight`:
  a real 2-cycle forces `A(P ∪ Q) % 8 = 5` when `2 ∈ P ∪ Q`, and
  `A(P ∪ Q) % 8 = 2` otherwise. This is the Lean-level version of the new
  two-adic union screen.
- `prime_three_mod_four_not_dvd_coprime_square_sum` and
  `twoCycle_union_A_not_dvd_prime_three_mod_four`: a real 2-cycle forces
  `A(P ∪ Q)=M(P)^2+M(Q)^2` with coprime side products, so no prime congruent
  to `3` modulo `4` can divide `A(P ∪ Q)`. This is another necessary union
  screen, not a nonexistence proof.
- `twoCycle_union_A_prime_dvd_two_mul_M_isSquare`,
  `twoCycle_union_A_prime_dvd_two_mul_M_legendre_one`, and
  `twoCycle_union_A_prime_dvd_primitive_norm_filter`: if an odd prime `l`
  divides `A(P ∪ Q)`, then `l % 4 = 1` and `2 * M(P ∪ Q)` has Legendre symbol
  `1` modulo `l`. This is the Lean-backed primitive norm divisor screen
  suggested by the latest GPT Pro pass.
- `twoCycle_union_prime` and `twoCycle_union_legendre_M_div_self_one`: helper
  lemmas converting the local square-residue condition into Legendre-symbol
  form for `M(P ∪ Q)/r` modulo each union prime `r`.
- `A_insert_of_notMem_prime`,
  `twoCycle_left_last_prime_recurrence`,
  `twoCycle_right_last_prime_recurrence`,
  `twoCycle_left_erase_core_equation`, and
  `twoCycle_right_erase_core_equation`: deleting a prime from one side gives
  the exact one-prime extension recurrence and the core equation
  `A(P0)A(Q)+M(P0)^2=M(P0)M(Q)`, with the symmetric right-side version.
- `twoCycle_of_left_core_extension` and `twoCycle_of_right_core_extension`:
  the converse core-extension direction is also Lean-proven. A disjoint core
  satisfying the core equation plus the forced-prime product equation extends
  back to a `TwoCycle`.
- `left_core_equiv_forced_product` and `right_core_equiv_forced_product`:
  under the forced quotient, the core equation is equivalent to the forced
  product equation. This makes the one-prime route a strict-gap problem:
  rule out `M(Q)=R*A(P0)+M(P0)` for every admissible largest-prime image core.
- `twoCycle_left_fixed_core_conditions` and
  `twoCycle_right_fixed_core_conditions`: deleting a chosen side prime from an
  actual `TwoCycle` gives the fixed `(S,R)` equations
  `A(Q)=R*M(S)` and `M(Q)=M(S)+R*A(S)`, with the symmetric right-side version.
- `twoCycle_of_left_fixed_core_extension` and
  `twoCycle_of_right_fixed_core_extension`: conversely, those fixed equations
  reconstruct a `TwoCycle` once the prime-set and disjointness hypotheses are
  supplied.
- `left_core_q_square_congruence`: every forced left core also satisfies
  `M(P0)+R*A(P0) == q*R*M(P0) mod q^2` for every `q ∈ Q`. This is the
  Lean-backed, subtraction-free form of the GPT Pro `q^2` local condition.
- `right_core_p_square_congruence`: the symmetric right-core version is also
  Lean-backed.
- `twoCycle_left_fixed_q_square_congruence` and
  `twoCycle_right_fixed_p_square_congruence`: the fixed `(S,R)` q-square screen
  is stated directly as a cycle-derived Lean corollary.

External arithmetic-derivative context:

Ufnarovski and Åhlander, "How to Differentiate a Number" (Journal of Integer
Sequences, 2003), state the general derivative formula `n' = n * sum e_i/p_i`,
prove the squarefree/coprime characterization, and explicitly identify the
period-2 case with the distinct-prime reciprocal equation. They present that
case as Conjecture 4, not as a solved theorem. No literature shortcut was found
there.

Additional parity draft:

```bash
PYTHONPATH=scripts python3 scripts/erdos307_parity_profile.py --prefix-size 17 --out examples/erdos-problems/attack/erdos307-parity-profile-draft.v1.json
PYTHONPATH=scripts python3 scripts/erdos307_parity_profile.py --verify examples/erdos-problems/attack/erdos307-parity-profile-draft.v1.json
```

This draft recomputes the parity law on all `131,072` subsets of the first `17`
primes with `0` violations. It also records the five necessary parity cases for
a cycle: if neither side contains `2`, both side cardinalities must be odd; if
one side contains `2`, the opposite side cardinality must be even. This is a
parity screen only, not a nonexistence proof.

Additional QR parity screen:

`scripts/erdos307_union_first_search.py` now includes a script-level global
quadratic-reciprocity parity screen. It uses the classical consequence of the
local square-residue constraints:

```text
binom(c,2) + epsilon*d == 0 mod 2
```

where `c` counts odd union primes congruent to `3 mod 4`, `epsilon` records
whether `2` is in the union, and `d` counts odd union primes congruent to
`3 or 5 mod 8`. The test suite checks this screen against the exact internal
Legendre filter on all subsets of the first `8` primes. This QR screen is not
yet Lean-formalized in `Erdos307.lean`.

Additional mod-16 and primitive norm divisor screens:

The latest GPT Pro pass did not identify a closing contradiction or known
finite bound. One follow-up suggested a small U-only mod-16 strengthening of the
square identities. `scripts/erdos307_union_first_search.py` now includes this
as `mod16_square_identity_pass`; the unit test confirms it is stricter than the
mod-8/two-adic screen on small examples, but the current `n=60..65` prefix-swap
artifact shows it is redundant on that bounded family.

The strongest new Lean-backed suggestion was the primitive norm divisor filter:
for a true cycle union, an odd prime divisor `l` of `A(U)` must satisfy
`l % 4 = 1` and `(2M(U) / l) = 1` in Legendre-symbol notation. This is now
Lean-backed in `Erdos307.lean` and wired into
`scripts/erdos307_union_first_search.py` as `primitive_norm_divisor_pass`.

Latest GPT Pro structural follow-up:

The follow-up again did not claim a solution. It said U-only QR reciprocity
gives only the already-scripted parity law, not a contradiction. It suggested a
small U-only mod-16 strengthening of the square identities as a next congruence
filter, but identified the one-prime deletion core as the cleanest
finite-reduction kernel:

```text
Core(P0,Q): A(P0)A(Q)+M(P0)^2 = M(P0)M(Q)
R = A(Q)/M(P0)
```

The Lean module now proves both directions needed for that scaffold: every
cycle yields this core equation after deleting a side prime, and any disjoint
core satisfying the core equation plus the forced-prime product equation extends
to a `TwoCycle`. This still needs a theorem or finite bound ruling out all such
cores.

Additional core-extension scan draft:

```bash
PYTHONPATH=scripts python3 scripts/erdos307_core_extension_search.py --prefix-size 16 --out examples/erdos-problems/attack/erdos307-core-extension-prefix16-draft.v1.json
PYTHONPATH=scripts python3 scripts/erdos307_core_extension_search.py --verify examples/erdos-problems/attack/erdos307-core-extension-prefix16-draft.v1.json
```

This draft scans every `Q` subset of the first `16` primes with `|Q| >= 2`,
factors `A(Q)`, takes the largest possible deleted prime `R`, and checks the
executable left-core certificate `A(P0)A(Q)+M(P0)^2=M(P0)M(Q)` with
`A(Q)=R M(P0)`. It recomputed cleanly: `65,519` `Q` subsets checked, `51,654`
squarefree `A(Q)` rows, `42,283` largest-`R` core candidates, `0` core equation
hits, `0` forced product hits, and `0` cycle hits. None of the `42,283`
candidates passed the Lean-backed `q^2` congruence screen. All `42,283`
largest-`R` candidates had positive forced-product gap
`M(Q)-(R*A(P0)+M(P0))`, with minimum gap `78` and maximum gap
`32,589,126,288,778,511,670`. A foreground `prefix_size=20` run was stopped
before writing an artifact because exact factorization over about one million
subsets was too slow for this iteration. This is bounded evidence only, not a
finite reduction.

Next #307 proof prompt:

```text
Prove or refute the strict-gap claim suggested by the core artifact:
if Q is a finite prime set, A(Q) is squarefree, its prime factors are disjoint
from Q, R is the largest prime factor of A(Q), P0 is the product of the other
prime factors, and R > max(Q), then M(Q) > R*A(P0)+M(P0).
By left_core_equiv_forced_product, this would rule out all largest-prime
left-core extensions. Look first for a counterexample with larger |Q| before
trying to formalize it.
```

Broader strict-gap stress artifact:

```bash
PYTHONPATH=scripts python3 scripts/erdos307_core_extension_search.py --prefix-size 35 --max-q-size 6 --out examples/erdos-problems/attack/erdos307-core-extension-first35-qsize6-draft.v1.json
PYTHONPATH=scripts python3 scripts/erdos307_core_extension_search.py --verify examples/erdos-problems/attack/erdos307-core-extension-first35-qsize6-draft.v1.json
```

This checks every `Q` subset of sizes `2..6` from the first `35` primes. It
recomputed cleanly: `2,007,292` `Q` subsets checked, `1,099,181` squarefree
`A(Q)` rows, `1,006,485` largest-`R` core candidates, all `1,006,485` with
positive forced-product gap, `0` q-square congruence passes, `0` zero gaps,
`0` negative gaps, `0` core equation hits, and `0` cycle hits. This remains
bounded evidence only.

Latest GPT Pro follow-up:

The most useful new idea is an exact `(S,R)`-first reduction. For
`a=M(S)`, `alpha=A(S)`, and `B_R=a+R*alpha`, a largest-prime core exists for
fixed `(S,R)` iff `B_R` is squarefree, every prime divisor of `B_R` is `< R`,
and the integer arithmetic derivative satisfies `D(B_R)=R*a`. Then `Q` is
forced to be the prime support of `B_R`. This removes `Q` as an independent
search variable and suggests formalizing an integer-derivative bridge for
`M(S)+R*A(S)`.

GPT Pro also suggested a local `q^2` condition for each `q | B_R`:

```text
R * (q*M(S) - A(S)) == M(S) mod q^2
```

The left-core form of this condition is now Lean-backed as
`left_core_q_square_congruence`, the symmetric right-core form is Lean-backed
as `right_core_p_square_congruence`, and the fixed-cycle corollaries are
Lean-backed as `twoCycle_left_fixed_q_square_congruence` and
`twoCycle_right_fixed_p_square_congruence`. The left-core screen is wired into
`scripts/erdos307_core_extension_search.py` as `q_square_congruence_pass_count`.
The fixed `(S,R)` equations are now Lean-backed in both directions and wired
into a standalone script. The support-product version of the GPT Pro exactness
criterion is now Lean-backed as `fixed_core_exact_iff`: for prime support `Q`
of `B_R`, if `M Q = a + R*alpha`, `alpha > 0`, `R` is prime, and every
`q in Q` is below `R`, then
`A Q = R*a` iff the local q-square checks hold and `R | A Q`. The inverse-sum
version is Lean-backed as `fixed_core_exact_iff_inverse_sum`, using
`R_dvd_A_iff_inverse_sum_zero` to replace `R | A Q` with
`sum_{q in Q} q^{-1} = 0` in `ZMod R`. The direct checker-shaped corollaries
are also Lean-backed as `fixed_SR_exact_iff_modular_checks` and
`fixed_SR_exact_iff_inverse_sum`, using `M Q = M S + R*A S` and deriving
`A S > 0` from `S.Nonempty`. The modular checks now reconstruct an actual
`TwoCycle` via `twoCycle_of_left_fixed_modular_extension`,
`twoCycle_of_left_fixed_inverse_sum_extension`, and symmetric right-side
theorems. The checker-shaped wrapper predicates `LeftFixedModularCandidate`,
`LeftFixedInverseSumCandidate`, `RightFixedModularCandidate`, and
`RightFixedInverseSumCandidate` now package these hypotheses, with matching
Lean reconstruction theorems to `TwoCycle`. The squarefree integer-support
bridge is also Lean-backed: `primeSet_primeFactors`,
`M_primeFactors_of_squarefree`, `fixed_SR_integer_exact_iff_modular_checks`,
`fixed_SR_integer_exact_iff_inverse_sum`, and
`twoCycle_of_left_fixed_integer_inverse_sum_candidate` convert an integer
`B_R=n` row with squarefree factorization into the forced support
`n.primeFactors` and reconstruct a `TwoCycle` if the q-square and inverse-sum
tests both hold. The checker's `D(B_R)` field now has a Lean-side definition
`D n = sum_{q in n.primeFactors} n/q`; `D_eq_A_primeFactors_of_squarefree`,
`fixed_SR_integer_D_exact_iff_modular_checks`,
`fixed_SR_integer_D_exact_iff_inverse_sum`, and
`twoCycle_of_left_fixed_integer_derivative_candidate` bridge exact
squarefree-derivative rows directly to `TwoCycle`. A full nonexistence proof or
finite bound remains open.
The cleaner local q-square form is Lean-backed as
`fixed_core_q_square_iff_alpha_divisor`: for `n=a+R*alpha`, `q | n`, and
`alpha` coprime to `q`, the congruence `n == q*R*a mod q^2` is equivalent to
`q | alpha*(n/q)+a^2`. The fixed-left certificate form is
`fixed_left_q_square_iff_core_divisor`, which proves
`M(Q) == q*R*M(S) mod q^2` iff
`q | A(S)*(M(Q)/q)+M(S)^2` under disjoint prime supports and
`M(Q)=M(S)+R*A(S)`. This packages the same local screen in a smaller divisor
form; it is not a nonexistence proof.
The latest GPT Pro fixed-core sharpening adds the missing largest-prime
residue: under the squarefree and all-factors-below-`R` hypotheses,
`D(B_R)=R*M(S)` is equivalent to the local q-square checks together with
`R | D(B_R)`. Equivalently, the forced support `Q` must satisfy
`sum_{q in Q} q^{-1} = 0` in `ZMod R`. The script now records both
`R_divides_integer_derivative_B_R` and `candidate_Q_inverse_sum_mod_R`; the
support-product inverse-sum criterion is Lean-backed, but this remains a bounded
obstruction rather than a solved nonexistence proof.
The same pass also isolated the signed defect: the q-square lifts imply
`A Q - R*a = k*M Q`, exactness is `k=0`, and the inverse-sum residue is the
defect modulo `R`. This is Lean-backed as
`fixed_core_signed_defect_exists_of_local` and
`fixed_core_exact_iff_signed_defect_zero`.
GPT Pro then suggested a residual-port recursion. The residual bridge is
Lean-backed as `fixed_core_residual_eq_of_exact` and
`fixed_core_exact_iff_residual_eq_of_fixed_product`: under
`M Q=M S+R*A S`, the equation
`M(S)*M(Q)=A(S)*A(Q)+M(S)^2` is equivalent to `A Q=R*M(S)`. The R-free theorem
`fixed_core_residual_eq_iff_exists_fixed_product` proves that, for nonempty
prime `S`, the residual equation is equivalent to the existence of a natural
`R` satisfying both fixed-core equations. Primality of `R` and
`R > max(S union Q)` remain separate side checks. The new checker
`scripts/erdos307_residual_port_search.py` enumerates fixed `S`, fixed `|Q|`
residual trees using the transition `(C,D)->(p*C-D,p*D)` and one-prime /
two-prime terminal tests. No full nonexistence proof was claimed, since there
is still no bound on `S` or on `|Q|`.
The checker terminal algebra is Lean-backed too:
`residual_one_prime_terminal_int` proves the one-prime endpoint,
`residual_two_prime_terminal_factorization_int` proves the positive-`C`
two-prime factor certificate, and `residual_transition_int` proves the
residual state transition over integers. The pruning theorem
`residual_positive_C_of_solution` proves that a positive-target residual
solution cannot have `C <= 0`.
Assessment-session evidence for the global-bound route:
`M_ge_pow_card_of_all_ge` proves `p^|Y| <= M(Y)` when every remaining prime is
at least `p`, and `p_mul_A_le_card_mul_M_of_all_ge` proves
`p*A(Y) <= |Y|*M(Y)`. The rational theorem
`residual_ratio_bound_of_least_prime` then proves the fixed-length next-prime
estimate
`C/D <= |Y|/p + a^2/(D*p^|Y|)`. This is theorem-grade support for fixed
`S`, fixed `|Q|` finite enumeration, but it still does not bound `|Q|`
uniformly.

The natural overbroad lemma "positive residual states force bounded support
length" is false without #307 origin hypotheses. Lean theorem
`residual_state_template` proves every finite support `Y` satisfies
`(A(Y)+M(Y))*M(Y)=M(Y)*A(Y)+M(Y)^2`; taking
`C=A(Y)+M(Y)`, `D=M(Y)`, and `a=M(Y)` gives exact residual states of arbitrary
support size. The missing hypothesis must use genuine fixed-core origin and
forced-prime side conditions, not only residual algebra, positivity, or the
reciprocal inequality.

Fixed-core origin with a natural forced `R` is also too weak unless the real
largest-prime side condition is included. Lean theorem
`fixed_core_R_one_of_A_eq_M_sub_one` proves that if `A(Q)=p`, `M(Q)=p+1`, and
`p` is prime, then singleton core `{p}` satisfies both fixed-core equations with
forced `R=1`. The concrete theorem `fixed_core_R_one_two_three_five` gives
`S={5}`, `Q={2,3}`, `R=1`. This row is excluded from #307 only because `R` is
not prime and not larger than the supports. Any viable global-bound lemma must
use `R` prime and `R > max(S union Q)`.

Largest-prime cardinal tradeoff:
`card_mul_M_lt_R_mul_A_of_primeSet_all_lt` proves that any nonempty prime
support `T` below `R` satisfies `|T| * M(T) < R * A(T)`. Combining this for a
true fixed-core row, theorem `fixed_core_card_tradeoff_lt_R_sq` proves
`|Q| * (|S| + 1) < R^2` from `M(Q)=M(S)+R*A(S)`, `A(Q)=R*M(S)`, nonempty
`S,Q`, and the below-`R` support hypotheses. This is the first Lean-backed
largest-prime cardinal tradeoff, but it still leaves `R` unbounded. So it
does not yield a finite reduction unless paired with an independent bound on
`R`, or with a theorem forcing `|S|` or `|Q|` to grow fast enough relative to
`R`.

Cycle-level form and assessment decision:
`twoCycle_left_largest_card_tradeoff_lt_R_sq` and
`twoCycle_right_largest_card_tradeoff_lt_R_sq` transport the tradeoff back to
actual `TwoCycle`s. If `R` is the largest support prime on either side, Lean
proves `|P| * |Q| < R^2`. This is theorem-grade evidence that the current
reciprocal/cardinality route remains `R`-dependent rather than global. Combined
with the arbitrary-length residual template and the `R=1` fixed-core-origin
counterexample, #307 should now be carried as partial reduction only. Do not
resume broader scans unless they test a named theorem that can bound `R`,
`|Q|`, or admissible cores `S`.

Additional fixed `(S,R)` scan draft:

```bash
PYTHONPATH=scripts python3 scripts/erdos307_fixed_sr_search.py --prefix-size 14 --max-s-size 5 --r-max 1000 --out examples/erdos-problems/attack/erdos307-fixed-sr-prefix14-s5-r1000-draft.v1.json
PYTHONPATH=scripts python3 scripts/erdos307_fixed_sr_search.py --verify examples/erdos-problems/attack/erdos307-fixed-sr-prefix14-s5-r1000-draft.v1.json
```

For fixed `S` and prime `R > max(S)`, the script checks
`B_R=M(S)+R*A(S)`. A largest-prime core for this fixed pair is forced exactly
when `B_R` is squarefree, all prime divisors of `B_R` are `<R`, and the integer
arithmetic derivative satisfies `D(B_R)=R*M(S)`; then `Q` is the prime support
of `B_R`. The artifact recomputed cleanly: `541,149` fixed `(S,R)` candidates
checked, `361,132` squarefree `B_R`, `15,896` squarefree rows with all factors
below `R`, `10` q-square congruence passes, `10` core-divisor condition passes,
`0` q-square/core-divisor mismatches, `0` q-square rows with `|S| != 1`, `0`
q-square survivors with `R | D(B_R)`, `45` candidate inverse-sum-zero rows
before the q-square conjunction, q-square derivative-gap range `[-33,060,
-44]`, `0` combined modular exact hits, `0` derivative-condition hits, and
`0` cycle hits. This is bounded evidence only, not a finite reduction. The
exact q-square plus `R | A Q` criterion and the equivalent core-divisor local
screen used to interpret these rows are Lean-backed.

Targeted non-singleton q-square profile:

```bash
PYTHONPATH=scripts python3 scripts/erdos307_fixed_sr_search.py --q-square-profile --prefix-size 18 --min-s-size 2 --max-s-size 4 --r-max 1000 --out examples/erdos-problems/attack/erdos307-fixed-sr-qsquare-nonsingleton-prefix18-s2-4-r1000-draft.v1.json
PYTHONPATH=scripts python3 scripts/erdos307_fixed_sr_search.py --verify examples/erdos-problems/attack/erdos307-fixed-sr-qsquare-nonsingleton-prefix18-s2-4-r1000-draft.v1.json
```

This profile recomputed cleanly: `616,794` fixed `(S,R)` candidates with
`2 <= |S| <= 4`, `482,381` squarefree `B_R`, `23,245` squarefree rows with all
factors below `R`, `0` q-square congruence passes, `51` candidate
inverse-sum-zero rows before the q-square conjunction, `0` combined modular
exact hits, `0` derivative-condition hits, and `0` cycle hits. This smaller
profile has been superseded as a proof prompt by the size-2 counterexamples
below.

Size-2 q-square survivor profile:

```bash
PYTHONPATH=scripts python3 scripts/erdos307_fixed_sr_search.py --q-square-profile --prefix-size 60 --min-s-size 2 --max-s-size 2 --r-max 10000 --out examples/erdos-problems/attack/erdos307-fixed-sr-qsquare-size2-prefix60-r10000-full-draft.v1.json
PYTHONPATH=scripts python3 scripts/erdos307_fixed_sr_search.py --verify examples/erdos-problems/attack/erdos307-fixed-sr-qsquare-size2-prefix60-r10000-full-draft.v1.json
```

This profile recomputed cleanly: `2,103,350` fixed `(S,R)` candidates with
`|S|=2`, `1,777,153` squarefree `B_R`, `591,325` squarefree rows with all
factors below `R`, `4` q-square congruence passes, `0` q-square survivors with
`R | D(B_R)`, `147` candidate inverse-sum-zero rows before the q-square
conjunction, `0` combined modular exact hits, `0` derivative-condition hits,
and `0` cycle hits. The first q-square survivor is
`S={2,5}`, `R=1559`, `B_R=10923=3*11*331`, with
`D(B_R)-R*M(S)=-B_R` and inverse-sum residue `1558 mod 1559`. The four
q-square survivors have derivative-gap quotients
`(D(B_R)-R*M(S))/B_R = -1, -12, -26, -44` and inverse-sum residues `1558`,
`1895`, `795`, and `185` modulo their respective `R`. Corrected proof prompt:
q-square does not force `|S|=1`; formalize the exact fixed-core criterion and
then prove the q-square plus largest-prime residue conjunction cannot hold, or
turn that conjunction into a finite certificate.

Residual-port certificate for the first q-square survivor's core:

```bash
PYTHONPATH=scripts python3 scripts/erdos307_residual_port_search.py --S 2,5 --q-size 3 --out examples/erdos-problems/attack/erdos307-residual-port-s2-5-q3-draft.v1.json
PYTHONPATH=scripts python3 scripts/erdos307_residual_port_search.py --verify examples/erdos-problems/attack/erdos307-residual-port-s2-5-q3-draft.v1.json
```

For `S={2,5}`, `a=10`, `alpha=7`, and `|Q|=3`, the residual tree has initial
state `(C,D)=(10,7)`. The finite next-prime bound leaves only least prime `3`.
After transition to `(C,D)=(23,21)`, the two-prime terminal integer is
`E=D^2+C*a^2=2741`, with factorization `2741`, and no divisor pair recovers a
valid prime pair. The artifact recomputed cleanly with `2` nodes visited, `1`
two-prime terminal row, `0` exact residual hits, and `0` cycle hits. This kills
the exact `|Q|=3` filling for that fixed core, including the q-square survivor
`Q={3,11,331}`, but it is not a global #307 proof.

Exact n=60 largest-core mass bound:

```bash
PYTHONPATH=scripts python3 scripts/erdos307_n60_mass_bound.py --out examples/erdos-problems/attack/erdos307-n60-mass-bound-draft.v1.json
PYTHONPATH=scripts python3 scripts/erdos307_n60_mass_bound.py --verify examples/erdos-problems/attack/erdos307-n60-mass-bound-draft.v1.json
```

For a largest-prime core with `|P ∪ Q|=60`, writing
`X=max(P0 ∪ Q)` gives
`X < 2/(2-H_58) = 1587.355944220109...`, where
`H_58=sum_{i<=58} 1/p_i = 1.998740043147044...`. Therefore the core maximum is
bounded by the largest prime below this value: `max(P0 ∪ Q) <= 1583`. This is a
theorem-grade finite-reduction component for the `n=60` largest-core case, but
the exhaustive finite check under that bound has not been run or formalized.

Additional bounded scan draft:

```bash
python3 scripts/erdos307_union_first_search.py --n-min 60 --n-max 65 --tail-extra 30 --max-swap-size 2 --external-filters 16 --out examples/erdos-problems/attack/erdos307-union-first-swap2-n60-65-draft.v1.json
python3 scripts/erdos307_union_first_search.py --verify examples/erdos-problems/attack/erdos307-union-first-swap2-n60-65-draft.v1.json --deep-verify
```

This draft deep-recomputed cleanly: `6` rows, `5,031,156` bounded union
candidates, `539,325` candidates passing the two-adic union screen, `539,325`
candidates passing the mod-16 square identity screen, `288,679` candidates
passing the global QR parity screen, `284,235` candidates passing the tested
`3 mod 4` divisor screen, `282,350` candidates passing the primitive norm
divisor screen, `0` witnesses, and `0` candidates passing the external modular
square/root filters. This remains bounded prefix-swap evidence only, not a
proof of nonexistence.

## #267 status

The useful bankable known territory is `c >= 2`, not the open subrange. A
Lean-green support module now exists at `lean/Vela/Erdos/Erdos267.lean` and is
imported by `lean/Vela.lean`.

Source check:

- Erdős Problems #267 states the Fibonacci reciprocal problem and says the main
  problem is proved for `c >= 2` by Badea, while `1 < c < 2` remains open.
- Badea 1987: `The irrationality of certain infinite series`, Glasgow Math. J.
  29(2), 221-228, DOI `10.1017/S0017089500006868`.
- Badea 1993: `A theorem on irrationality of infinite series and applications`,
  Acta Arithmetica 63, 313-323, DOI `10.4064/aa-63-4-313-323`.
- Citation verdict: the draft's theorem input should be Badea 1993,
  Corollary 3.2, for the Fibonacci/growth-condition application. The 1987
  Glasgow paper is related background and should not replace the 1993 citation
  in the banked c>=2 record.

Lean status:

- `lake build Vela.Erdos267` passed.
- `rg -n "sorry|axiom|unsafe|admit" lean/Vela/Erdos/Erdos267.lean lean/Vela.lean`
  returned no matches.
- The module defines the Fibonacci reciprocal subseries using `Nat.fib`.
- The module proves that eventual ratio `c <= n(k+1)/n(k)` with `c >= 2`
  implies Badea's eventual growth condition `2*n(k)-1 <= n(k+1)`.
- The module proves that Badea 1993 Corollary 3.2, supplied as a published
  theorem input, gives the known `c >= 2` statement.

Draft record:

- `examples/erdos-problems/attack/erdos267-badea-ge-two-lean-bridge-record-draft.v1.json`
- `deliverable_grade`: `partial_proof`
- `grade_gate`: `ok`
- `gate_status`: `verified`

Do not bank this as a solve. It says only that the `c >= 2` territory is a
known theorem-backed partial and that the mission's requested `1 < c < 2`
subrange remains open.

## #463 status

Primary after the refocus. No full solve was found.

Working criterion:

`g(n)=max{d>0 : n+d is composite and d < minFac(n+d)}`. The official question is
YES iff `g(n)->infinity`. The existing reduction was treated as prior work.

Official-window bridge:

- `lean/Vela/Erdos/Erdos463.lean` now also defines `OfficialWindow` and
  `OfficialLargeWindow`.
- New Lean theorem `officialWindow_iff_exists_roughOffset`: the official fixed
  threshold condition `n+K<m<n+p(m)` with `m` composite is equivalent to an
  offset `d>K` satisfying `RoughCompositeOffset n d`.
- New Lean theorem `officialLargeWindow_iff_large_rough_offsets`: the official
  eventual large-window target is exactly `HasLargeRoughCompositeOffsets`.
- Draft record:
  `examples/erdos-problems/attack/erdos463-official-window-bridge-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos463_official_window_bridge.py`.
- Proposed grade: `verified_reduction`.
- Caveat: this pins the residual theorem; it does not prove the missing
  all-large-`n` rough-composite input.

F-witness bridge:

- `lean/Vela/Erdos/Erdos463.lean` now also defines `ErdosFWitness`, the existential
  form of Erdos's auxiliary `F(N)=min_{m>N}(m-p(m))` language without defining
  the minimum itself.
- New Lean theorem `composite_window_iff_erdosF_ineq`: for the composite
  official witness `m`, the window inequality `m<n+minFac(m)` is equivalent to
  `m-minFac(m)<n`.
- New Lean theorem `officialWindow_iff_erdosFWitness`: the official
  fixed-threshold condition at `(K,n)` is equivalent to the existential
  statement that some composite `m>n+K` satisfies `m-minFac(m)<n`.
- Draft record:
  `examples/erdos-problems/attack/erdos463-f-witness-bridge-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos463_f_witness_bridge.py`.
- Proposed grade: `verified_reduction`.
- Caveat: this matches the official page's auxiliary-function phrasing but
  does not prove `OfficialLargeWindow`, `Tendsto roughMargin atTop atTop`, or
  any bound for the auxiliary function.

Shifted-sieve criterion:

- `lean/Vela/Erdos/Erdos463.lean` now also defines
  `SmallPrimeAvoidingCompositeOffset` and
  `HasLargeSmallPrimeAvoidingCompositeOffsets`.
- New Lean theorem `roughCompositeOffset_iff_smallPrimeAvoidingCompositeOffset`:
  for fixed `n,d`, `d < minFac(n+d)` with `n+d` composite is equivalent to
  `n+d` being composite and no prime `p<=d` dividing `n+d`.
- New Lean theorem
  `officialLargeWindow_iff_large_smallPrimeAvoidingCompositeOffsets`: the
  official target is exactly eventual existence of arbitrarily large shifted
  sieve offsets.
- New Lean theorem
  `tendsto_roughMargin_iff_large_smallPrimeAvoidingCompositeOffsets`: the
  formal margin diverges exactly when those shifted-sieve offsets exist.
- Draft record:
  `examples/erdos-problems/attack/erdos463-shifted-sieve-criterion-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos463_shifted_sieve_criterion.py`.
- Proposed grade: `verified_reduction`.
- Caveat: this is an exact residual reformulation. It still leaves the
  all-large-`n` existence theorem open.

New Lean support:

- `lean/Vela/Erdos/Erdos463.lean`
- Theorem `offset_square_bound`: if `n+d` is composite and `d < minFac(n+d)`,
  then `d^2+d+1 <= n`.
- Corollary `offset_le_sqrt`: every qualifying offset satisfies
  `d <= floor(sqrt(n))`.
- New theorem `sqrt_prime_square_sub_prime_add_one`: for every prime `p`,
  `sqrt(p^2-p+1)=p-1`.
- New theorem `roughCompositeOffset_prime_square_sub_prime_add_one`: at
  `n=p^2-p+1`, the offset `p-1` reaches the square `p^2` and is a valid
  rough-composite offset.
- New theorem `roughMargin_eq_sqrt_prime_square_sub_prime_add_one`: for every
  prime `p`, `roughMargin(p^2-p+1)=floor(sqrt(p^2-p+1))=p-1`, so the universal
  square-root upper bound is sharp at those points.
- New theorem `exists_arbitrarily_large_roughMargin_eq_sqrt`: such saturation
  points occur arbitrarily far out.
- Draft record:
  `examples/erdos-problems/attack/erdos463-square-saturation-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos463_square_saturation.py`.
- Proposed grade: `partial_proof`.
- Caveat: this proves exact limsup saturation of the search-window upper bound.
  It still does not prove the eventual all-`n` divergence target.

Finite-prefix artifact:

- `examples/erdos-problems/attack/erdos463-margin-prefix-10m-draft.v1.json`
- `scripts/erdos463_margin_search.py`
- Exact recomputation for `1 <= n <= 10000000`, checking only
  `d <= floor(sqrt(n))` by the Lean lemma.
- Summary: `zero_g_count=90`, `last_zero_g_n=19380`,
  `count_g_le_3=769`, `last_g_le_3_n=267380`.
- Tail minimum on `[5000000,10000000]`: `g(5266180)=877`, witnessed by
  `m=5267057` with `minFac(m)=1427`.

Draft record:

- `examples/erdos-problems/attack/erdos463-margin-prefix-obstruction-record-draft.v1.json`
- `deliverable_grade`: `obstruction_map`
- `grade_gate`: `ok`
- `gate_status`: `verified`

Caveat:

This is not a solve. The data supports the expected growth but does not supply
the missing uniform theorem on rough composites in short intervals. The
finite-prefix checker should be banked only as an obstruction/evidence map.

Published theorem closure check:

- `examples/erdos-problems/attack/erdos463-published-rough-input-gap-record-draft.v1.json`
- Gafni-Tao 2025 proves rough numbers in almost all prime gaps, with
  `O(X/log^2 X)` exceptional gaps.
- This is strong context for #463 but not a solve-bar theorem for the reduction:
  #463 needs every sufficiently large `n` and a margin tending to infinity, not
  an almost-all statement over prime gaps.

Semiprime-gap bridge:

- `lean/Vela/Erdos/Erdos463.lean` now also defines `SemiprimeOffset` and
  `UniformSemiprimeGapHypothesis`, the large-offset criterion
  `HasLargeRoughCompositeOffsets`, plus the formal margin `roughMargin`.
- New Lean theorem `tendsto_roughMargin_iff_large_rough_offsets`: the formal
  margin tends to infinity exactly when the large rough-composite offset
  criterion holds.
- New Lean theorem `semiprimeOffset_rough`: if `n+d=p*q` with `p<=q` prime and
  `d<p`, then the offset is a valid `RoughCompositeOffset`.
- New Lean theorem `uniformSemiprimeGapHypothesis_gives_large_rough_offsets`:
  the uniform threshold version of that restricted semiprime-gap input implies
  arbitrarily large rough-composite offsets.
- New Lean theorems `le_roughMargin_of_offset`,
  `large_rough_offsets_tendsto_roughMargin`, and
  `uniformSemiprimeGapHypothesis_tendsto_roughMargin`: the formal max-margin
  function dominates any valid offset, and the same uniform semiprime-gap input
  would imply `Tendsto roughMargin atTop atTop`.
- New Lean theorem `primeGapRough_covers_left_prefix`: a rough composite inside
  a prime-gap-style interval covers the left prefix far enough below it. This
  formalizes why almost-all prime-gap theorems are relevant evidence but do not
  close the all-`n` #463 quantifier.
- Draft record:
  `examples/erdos-problems/attack/erdos463-semiprime-gap-bridge-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos463_semiprime_bridge.py`.
- Proposed grade: `partial_proof`.
- Caveat: the hard analytic theorem remains missing. Tao's #385 note identifies
  exactly this kind of uniform restricted-semiprime gap input as the right
  shape, and also explains the parity/Siegel-zero obstruction. This record does
  not prove the official #463 question.

Fixed-offset obstruction:

- `lean/Vela/Erdos/Erdos463.lean` now also proves
  `not_roughCompositeOffset_of_offset_dvd`: if `1<d` and `d | n`, then `d`
  cannot be a valid `RoughCompositeOffset n d`.
- New Lean theorem `not_roughCompositeOffset_le_of_factorial_mul`: for every
  fixed window `2 <= d <= D`, all numbers `n = D! * t` defeat that entire
  finite offset window.
- New Lean theorem `not_roughCompositeOffset_le_of_lcmUpto_mul`: the same
  obstruction holds for the exact common multiple
  `n = lcm(1,...,D) * t`, sharpening the factorial barrier.
- New Lean theorem `roughCompositeOffset_lcmUpto_mul_eq_one_or_gt`: on such
  an lcm multiple, every valid rough-composite offset is either `d=1` or
  `d>D`.
- Draft record:
  `examples/erdos-problems/attack/erdos463-factorial-offset-obstruction-record-draft.v1.json`.
- Sharper lcm draft record:
  `examples/erdos-problems/attack/erdos463-lcm-offset-obstruction-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos463_factorial_obstruction.py`.
- Sharper lcm verifier:
  `scripts/verify_erdos463_lcm_obstruction.py`.
- Proposed grade: `obstruction_map`.
- Caveat: this rules out fixed finite-offset certificate strategies. It does
  not supply the required growing offsets and does not prove the official #463
  question.

Balanced-window and coprime obstruction bridge:

- `lean/Vela/Erdos/Erdos463.lean` now also defines `BalancedSemiprimeWindow` and
  `UniformBalancedSemiprimeWindowHypothesis`.
- New Lean theorem `balancedSemiprimeWindow_gives_roughOffset`: if
  `(n+K,n+H]` contains a semiprime `m=p*q` with `p<=q` and `H<p`, then
  `d=m-n` is a valid rough-composite offset larger than `K`.
- New Lean theorem
  `uniformBalancedSemiprimeWindowHypothesis_gives_large_rough_offsets`: the
  uniform version of that balanced-window input implies the large-offset
  criterion.
- New Lean theorem
  `uniformBalancedSemiprimeWindowHypothesis_officialLargeWindow`: the same
  input implies the official large-window formulation through the already
  verified rough-offset equivalence.
- New Lean theorem
  `uniformBalancedSemiprimeWindowHypothesis_tendsto_roughMargin`: the same
  input implies divergence of the formal `roughMargin` function.
- New Lean theorem `not_roughCompositeOffset_of_common_prime_dvd`: if a prime
  divides both `n` and `d`, then `d` cannot be a rough-composite offset from
  `n`.
- New Lean theorem `coprime_of_roughCompositeOffset`: every valid
  rough-composite offset is coprime to `n`.
- Draft record:
  `examples/erdos-problems/attack/erdos463-balanced-window-coprime-obstruction-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos463_balanced_window_coprime.py`.
- Proposed grade: `obstruction_map`.
- Caveat: Iwaniec-Laborde `P2` in short intervals is still insufficient as a
  closure theorem because it does not guarantee composite balanced roughness.
  The missing input is a uniform restricted semiprime-gap theorem of the shape
  Tao identifies for #385.

Floor-successor prime criterion:

- `lean/Vela/Erdos/Erdos463.lean` now also defines
  `CeilPrimeSemiprimeCriterion` and
  `UniformCeilPrimeSemiprimeCriterion`.
- New Lean theorem `ceilPrimeSemiprimeCriterion_gives_semiprimeOffset`: if
  `p` is prime, `p ∤ n`, `K < p - n % p`, `n / p + 1` is prime, and
  `p <= n / p + 1`, then the next multiple
  `p * (n / p + 1)` gives a semiprime offset beyond `K`.
- New Lean theorem `semiprimeOffset_gives_ceilPrimeSemiprimeCriterion`:
  conversely, every semiprime offset beyond `K` arises from its smaller prime
  factor through this floor-successor criterion.
- New Lean theorem
  `exists_ceilPrimeSemiprimeCriterion_iff_exists_semiprimeOffset`: for fixed
  `n,K`, the floor-successor prime criterion is exactly equivalent to the
  semiprime-offset target.
- New Lean theorem `ceilPrimeSemiprimeCriterion_gives_roughOffset`: the same
  criterion gives a valid #463 rough-composite offset.
- New Lean theorem `ceilPrimeSemiprimeCriterion_lt_roughMargin`: every such
  witness gives `K < roughMargin n`.
- New Lean theorems
  `uniformCeilPrimeSemiprimeCriterion_gives_large_rough_offsets`,
  `uniformCeilPrimeSemiprimeCriterion_iff_uniformSemiprimeGapHypothesis`,
  `uniformCeilPrimeSemiprimeCriterion_officialLargeWindow`, and
  `uniformCeilPrimeSemiprimeCriterion_tendsto_roughMargin`: the uniform
  criterion is equivalent to the semiprime-gap residual and implies the
  official large-window target plus
  divergence of `roughMargin`.
- Draft record:
  `examples/erdos-problems/attack/erdos463-ceil-prime-semiprime-criterion-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos463_ceil_prime_semiprime_criterion.py`.
- Proposed grade: `partial_proof`.
- Caveat: this exactly sharpens the semiprime route to a quotient/remainder
  prime criterion. It does not prove the uniform existence theorem or close
  #463.

Limsup margin and lcm escape:

- `lean/Vela/Erdos/Erdos463.lean` now also proves
  `exists_arbitrarily_large_roughMargin_gt`: for every thresholds `N,K`, there
  is some `n>N` with `K<roughMargin n`.
- Stronger block theorem:
  `exists_arbitrarily_long_roughMargin_gt_block`: for every thresholds `N,K,L`,
  there is a start `s>N` such that every `n` in the block `s<=n<s+L` has
  `K<roughMargin n`.
- The proof is the explicit semiprime construction `d=K+1`,
  `n=p*q-d`, with primes `p>d` and `q` large. Then `n+d=p*q` and
  `d<p`, so the semiprime bridge gives a valid rough-composite offset.
- The block proof uses one semiprime `m=p*q` to cover the full interval
  `m-p<n<m-K` once `p>K+L`.
- `lean/Vela/Erdos/Erdos463.lean` also proves
  `roughCompositeOffset_of_lcmUpto_dvd_escape_prime`: on a multiple of
  `lcm(1,...,D)`, a prime offset `q>D` is valid once `n+q` is composite and no
  prime in the band `D<r<=q` divides it. This explains how the lcm obstruction
  can be escaped just beyond the fixed forbidden window.
- Draft record:
  `examples/erdos-problems/attack/erdos463-limsup-margin-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos463_limsup_margin.py`.
- Proposed grade: `partial_proof`.
- Caveat: this proves only unbounded limsup-style behavior and arbitrarily long
  good blocks for the formal margin. It does not prove
  `Tendsto roughMargin atTop atTop` or the official all-large-`n` #463 target.

## #686 status

Partial proof/certificate work only. No full solve was found.

The current counterexample route for #686 focuses on square `N`, especially
`N=4`. The live thread records small-`k` progress and finite searches, but not
an all-`k` obstruction. The local Lean theorem closes the `k=2` and `k=4`
branches for prime-power square roots. A later GPT Pro pass supplied usable
local certificate material for `k=3,6,8`, now verified by script but not
promoted to a full solve.

Lean file:

- `lean/Vela/Erdos/Erdos686.lean`

New theorems:

- `no_prime_power_square_consecutive_product`: if `A` is a prime power, then
  `A^2 * N * (N+1) = M * (M+1)` is impossible for positive `N`.
- `no_k2_prime_power_square`: in the #686 two-factor indexing, `A^2` has no
  `k=2` representation whenever `A` is a prime power.
- `no_k2_four`: concrete corollary that `N=4` has no `k=2` representation.
- `four_factor_product_eq_four_mul_two_factor`: the algebraic collapse from a
  four-factor product to a two-factor product at `u=t(t+5)/2+1`.
- `k4_equation_reduces_to_k2`: every `k=4` product equation induces a `k=2`
  product equation with the same multiplier.
- `no_k4_prime_power_square`: a `k=4` representation of a prime-power square
  would induce a forbidden `k=2` representation.
- `no_k4_four`: concrete corollary that `N=4` has no `k=4` representation.
- `shift_dvd_three_mul_intervalProd`: if
  `intervalProd (n+d) k = 4*intervalProd n k`, then
  `d | 3*intervalProd n k`.
- `intervalProd_pred_succ`: moving an interval-product base one step left adds
  the missing first factor.
- `intervalProd_local_shift_dvd`: if
  `intervalProd (n+d) k = C*intervalProd n k`, `k<=d`, and `1<=r<=k`, then
  `n+r | intervalProd (d-r) k`.
- `prime_dvd_intervalProd_exists_term`: if a prime divides an interval product,
  it divides one of the interval terms.
- `lower_prime_factor_source_residue_of_eq`: every prime factor of a lower
  factor `n+r` must divide one shifted source term `d-r+j`.
- `no_eq_of_lower_prime_factor_missing_source_residue`: contradiction form of
  the lower source residue filter.
- `sourcePrefixProd`: product of the shifted source intervals for `r=1..j`.
- `intervalProd_dvd_sourcePrefixProd_of_local_shift`: for `j<=k`, `P_j(n)`
  divides the product of the first `j` shifted source intervals.
- `intervalProd_dvd_sourcePrefixProd_full_of_local_shift`: the full lower
  product `P_k(n)` divides the product of all shifted source intervals.
- `gapIntervalProd`: the gap-side product `product_{j=1..k}(x+2*r-j)`, written
  as an interval product.
- `gapReverseProd`: reverse-order gap-side product, induction-friendly for the
  signed congruence.
- `upperReverseProd`: reverse-order upper-side product
  `product_{j=1..k}(d+s-j)`, induction-friendly for the upper local filter.
- `shifted_source_add_gap_factor`: with `x=n-d`, the paired factors satisfy
  `(d-r+j)+(x+2*r-j)=n+r`.
- `gap_factor_pos`: the gap-side factors are positive when `k<x`.
- `gapIntervalProd_last_factor`: the final gap factor is `x+2*r-1`.
- `gapIntervalProd_succ`: increasing the gap-product length adds the new
  lowest gap factor.
- `gapReverseProd_eq_gapIntervalProd`: the reverse gap product equals the
  interval-style gap product.
- `upperReverseProd_eq_intervalProd`: the reverse upper product equals
  `intervalProd (d+s-k-1) k`.
- `source_gapReverseProd_modEq_int`: over `Int.ModEq`,
  `P_j(d-r) ≡ (-1)^j*gapReverseProd(x,r,j) mod (n+r)`.
- `lower_upperReverseProd_modEq_int`: for every `M | n+d+s`, over `Int.ModEq`,
  `P_j(n) ≡ (-1)^j*upperReverseProd(d,s,j) mod M`.
- `lower_factor_dvd_gapReverseProd_of_source_dvd`: transfers
  `n+r | P_k(d-r)` to `n+r | gapReverseProd(x,r,k)`.
- `lower_factor_dvd_gapIntervalProd_of_source_dvd`: transfers
  `n+r | P_k(d-r)` directly to `n+r | gapIntervalProd(x,r,k)`.
- `upper_factor_div_gcd_dvd_lower_interval_of_eq_four`: cancels the multiplier
  `4` from an upper factor, proving
  `(n+d+s)/gcd(n+d+s,4) | intervalProd n k`.
- `upper_factor_dvd_upperReverseProd_of_eq_four`: upper local filter in
  reverse-product form:
  `(n+d+s)/gcd(n+d+s,4) | upperReverseProd d s k`.
- `upper_factor_dvd_upperIntervalProd_of_eq_four`: upper local filter in
  interval-product form:
  `(n+d+s)/gcd(n+d+s,4) | intervalProd (d+s-k-1) k`.
- `upper_prime_factor_residue_of_eq_four`: every prime factor of the upper
  quotient `(n+d+s)/gcd(n+d+s,4)` divides one upper difference term
  `d+s-k-1+j`.
- `no_eq_four_of_upper_prime_factor_missing_residue`: contradiction form of the
  upper residue filter.
- `prime_not_dvd_gapReverseProd_of_last_lt`: a prime above `x+2*r-1` cannot
  divide the reverse gap product.
- `upper_quotient_prime_le_tail_of_eq_four`: if the upper quotient
  `(n+d+s)/gcd(n+d+s,4)` is prime, then it is at most `d+s-1`.
- `no_eq_four_of_lower_prime_gt_shift_tail`: if additionally `n+r` is prime and
  `d-r+k < n+r`, then the `N=4` interval equation is impossible.
- `no_eq_four_of_lower_prime_factor_gt_shift_tail`: if a prime factor `p` of
  `n+r` satisfies `d-r+k < p`, then the `N=4` interval equation is impossible.
- `lower_prime_factor_le_shift_tail_of_eq_four`: every prime divisor `p` of a
  lower factor `n+r` satisfies `p<=d-r+k`.
- `no_eq_four_of_lower_prime_of_d_add_k_lt_n`: if `d+k<n` and any lower-block
  factor `n+r` with `1<=r<=k` is prime, then the equation is impossible.
- `lower_block_prime_forces_n_le_d_add_k`: any hypothetical solution with a
  prime in `[n+1,n+k]` must satisfy `n<=d+k`.
- `no_eq_four_of_near_diagonal_ge_five`: `n<=d+k` is impossible in a separated
  `N=4` interval equation once `k>=5`.
- `no_lower_block_prime_of_eq_four_ge_five`: every remaining hypothetical
  `N=4,k>=5` solution has no prime in `[n+1,n+k]`.
- `exists_lower_block_prime_of_n_le_k`: Bertrand gives a lower-block prime when
  `n<=k` and `k>=2`.
- `eq_four_ge_five_forces_k_lt_n`: every remaining separated `N=4,k>=5`
  solution must satisfy `k<n`.
- `eq_four_ge_five_forces_d_add_k_lt_n`: every remaining separated
  `N=4,k>=5` solution must satisfy `d+k<n`.
- `eq_four_ge_five_forces_two_mul_k_lt_n`: every remaining separated
  `N=4,k>=5` solution must satisfy `2*k<n`.
- `eq_four_ge_five_forces_d_lt_n_add_k`: every remaining separated `N=4,k>=5`
  solution must satisfy `d<n+k`.
- `eq_four_ge_five_remaining_window`: bundles the current all-`k` reduction:
  from `P_k(n+d)=4P_k(n)`, `k<=d`, and `k>=5`, any remaining row has `k<n`,
  `2*k<n`, `d+k<n`, `d<n+k`, `d | 3*P_k(n)`, and no prime in `[n+1,n+k]`.
- `lower_factor_dvd_smaller_intervalProd_of_eq_four_ge_five`: in the
  remaining `k>=5` window, each `n+r` divides `intervalProd (d-r) k` and
  `d-r+k<n+r`.
- `lower_factor_dvd_gapReverseProd_of_eq_four_ge_five`: in the remaining
  `k>=5` window, each `n+r` divides `gapReverseProd (n-d) r k`.
- `lower_factor_dvd_gapIntervalProd_of_eq_four_ge_five`: in the remaining
  `k>=5` window, each `n+r` divides `gapIntervalProd (n-d) r k`.
- `lower_prime_factor_gap_residue_of_eq_four_ge_five`: every prime factor of a
  lower factor `n+r` must divide one gap interval term
  `n-d+2*r-k-1+j`.
- `no_eq_four_of_lower_prime_factor_missing_gap_residue`: contradiction form of
  the lower gap residue filter.
- `lower_factor_dvd_source_gap_gcd_of_eq_four_ge_five`: in the remaining
  `k>=5` window, each `n+r` divides the gcd of the source product
  `intervalProd (d-r) k` and the gap product `gapIntervalProd (n-d) r k`.
- `no_eq_four_of_source_gap_gcd_lt_lower_factor`: if one such source-gap gcd is
  smaller than `n+r`, the remaining row is impossible.
- `lower_prime_factor_le_gap_tail_of_eq_four_ge_five`: every prime divisor
  `p` of `n+r` satisfies the gap-tail bound `p<=n-d+2*r-1`.
- `lower_prime_factor_le_dual_tail_of_eq_four_ge_five`: every prime divisor
  `p` of `n+r` satisfies `p<=min(d-r+k,n-d+2*r-1)`.
- `lower_prime_factor_lt_lower_factor_of_eq_four_ge_five`: in the remaining
  `k>=5` window, every prime divisor of `n+r` is strictly smaller than `n+r`.
- `scripts/verify_erdos686_dual_tail_counterexample.py`: exact verifier showing
  the dual-tail prime-factor bounds alone are not a contradiction. The checked
  state is `k=5,d=11,x=12,n=23`; all dual bounds hold, but the local divisor
  filter fails at `r=2`.
- `scripts/verify_erdos686_gcd_filter_prefix.py`: bounded verifier for the
  source-gap gcd obstruction. It found no survivors for `5<=k<=16`,
  `k<=d<=250`, `k<x<=250`. This is prefix evidence only.
- `scripts/verify_erdos686_gptpro_partial.py`: exact checker for GPT Pro's
  partial counterexamples to weaker global packages. It verifies the states
  `k=5,n=23,d=8`, `k=5,n=47,d=16`, and `k=5,n=183,d=60`; they are not #686
  solutions, but they show smoothness-only packages cannot replace the exact
  local filters.
- `scripts/verify_erdos686_combined_local_filters.py`: bounded checker for the
  lower source-gap gcd filters plus the upper exact filters. The checked
  remaining-window prefix `5<=k<=18`, `k<=d<=350`, `k<x<=350`, `d<2*x` has no
  lower-gcd survivor and no combined survivor.
- `filterMatrixCell`: shared lower/upper exact-filter matrix cell
  `gcd(d+s-j, n-d+2*j-s)`, following the GPT Pro matrix formulation.
- `gcd_two_mul_add_add` and `filterMatrixCell_eq_gcd_blocks`: Lean-backed block
  identity for matrix cells. With `n=d+x`,
  `gcd(d+s-j,n-d+2*j-s)=gcd(n+d+s,n+j)`.
- `gcdIntervalProd`, `gcd_intervalProd_dvd_gcdIntervalProd`, and
  `dvd_gcdIntervalProd_of_dvd_intervalProd`: generic termwise-gcd product
  bookkeeping. If `M` divides an interval product, then `M` divides the product
  of the termwise gcds with `M`.
- `filterMatrixColumnProd`,
  `lower_factor_dvd_filterMatrixColumnProd_of_source_dvd`, and
  `lower_factor_dvd_filterMatrixColumnProd_of_eq_four_ge_five`: the GPT Pro
  lower matrix column filter is Lean-backed. In every remaining `N=4,k>=5` row,
  each lower factor `n+r` divides `prod_{s=1..k} gcd(d+s-r,n-d+2*r-s)`.
- `no_eq_four_of_filterMatrixColumnProd_lt_lower_factor`: if one such lower
  column product is smaller than `n+r`, the remaining row is impossible.
- `filterMatrixRowProd`, `gcd_upperTerm_dvd_filterMatrixCell`,
  `gcd_upperReverseProd_dvd_filterMatrixRowProd`,
  `upper_factor_dvd_filterMatrixRowProd_of_eq_four_ge_five`: the GPT Pro upper
  matrix row filter is Lean-backed. In every remaining `N=4,k>=5` row, each
  upper quotient `(n+d+s)/gcd(n+d+s,4)` divides
  `prod_{j=1..k} gcd(d+s-j,n-d+2*j-s)`.
- `no_eq_four_of_filterMatrixRowProd_lt_upper_quotient`: if one such row product
  is smaller than the corresponding upper quotient, the remaining row is
  impossible.
- `no_eq_four_of_matrix_size_obstruction_ge_five`: combined contradiction
  wrapper for any concrete lower-column or upper-row matrix-size failure.
- `no_eq_four_ge_five_of_forall_matrix_size_obstruction`: formal contract for
  the remaining global matrix route. A proof of the pure matrix-size lemma for
  all `k>=5`, `k<=d`, `k<x` closes every remaining `N=4,k>=5` row in Lean.
- `filterMatrix_large_prime_unique` and `filterMatrixCell_large_prime_unique`:
  if a prime `p>k` divides the defining factors, respectively the cells, of two
  matrix entries in the remaining `k x k` block, then the row and column agree.
  This banks large-prime p-adic bookkeeping for the remaining global matrix
  contradiction route.
- `scripts/verify_erdos686_matrix_size_filters.py`: bounded verifier for the
  global matrix-size candidate. It tests whether every remaining-window state
  has either a lower column product smaller than `n+r` or an upper row product
  smaller than `(n+d+s)/gcd(n+d+s,4)`.
- `examples/erdos-problems/attack/erdos686-matrix-size-prefix-k24-d299-x299-draft.v1.json`:
  verified artifact for `5<=k<=24`, `k<=d<=299`, `k<x<=299`, checking
  `1,625,160` states. It has no combined matrix-size survivor. It also records
  the first lower-column-only size counterexample: `k=13,d=50,x=63,n=113`,
  where all lower column products are at least `n+r`, but upper row `s=4` has
  product `1 < 167`.
- Follow-up prompt:
  `docs/erdos-attack/686/gpt-pro-matrix-size-global-prompt.md`.
- `scripts/verify_erdos686_factorial_ratio.py`: bounded verifier for the newer
  factorial-slack global candidate. The proposed route uses
  `prod_i gcd(M,y+i) | M*k!` to reduce the Lean-backed matrix filters to the
  condition that the reduced ratio `P_Q/P_A`, with
  `P_A=prod_j(n+j)` and
  `P_Q=prod_s (n+d+s)/gcd(n+d+s,4)`, has numerator dividing `(k!)^k` and
  denominator dividing `4^k*(k!)^k`.
- `examples/erdos-problems/attack/erdos686-factorial-ratio-prefix-k24-d299-x299-draft.v1.json`:
  verified artifact for `5<=k<=24`, `k<=d<=299`, `k<x<=299`, checking
  `1,625,160` states. It has `first_survivor = null`; every checked state
  already fails numerator divisibility. This is not a proof until the factorial
  gcd-product lemma, ratio-slack implication, and all-`k` pure-window
  obstruction are proved.
- Follow-up prompt:
  `docs/erdos-attack/686/gpt-pro-factorial-ratio-obstruction-prompt.md`.
- New Lean contradiction wrappers:
  `no_eq_four_of_odd_prime_factorization_surplus` and
  `no_eq_four_of_large_prime_factorization_surplus`. Since
  `intervalProd_factorization_eq_of_odd_prime` already says odd-prime
  valuations agree under `P_k(n+d)=4*P_k(n)`, any prime `p>k` with larger
  valuation in the upper interval product than the lower interval product now
  closes the row directly.
- New Lean contract theorem:
  `no_eq_four_ge_five_of_forall_large_prime_factorization_surplus`. A proof of
  the pure large-prime surplus theorem for all `k>=5`, `k<=d`, `k<x` closes
  every remaining `N=4,k>=5` row in Lean.
- `scripts/verify_erdos686_large_prime_surplus.py`: bounded verifier for the
  sharper pure-window theorem that every remaining state has some prime `p>k`
  with `v_p(P_k(n+d)) > v_p(P_k(n))`.
- `examples/erdos-problems/attack/erdos686-large-prime-surplus-prefix-k24-d299-x299-draft.v1.json`:
  verified artifact for `5<=k<=24`, `k<=d<=299`, `k<x<=299`, checking
  `1,625,160` states. It has `first_survivor = null`; the first checked state
  `k=5,d=5,x=6,n=11` already has surplus primes `17` and `19`.
- Follow-up prompt:
  `docs/erdos-attack/686/gpt-pro-large-prime-surplus-prompt.md`.
- New Lean diagonal-route definitions and contracts:
  `filterMatrixRowsProd`, `filterMatrixColumnsProd`, `upperQuotientProd`,
  `diagonalDelta`, `diagonalH`,
  `no_eq_four_of_diagonal_lcm_obstruction`, and
  `no_eq_four_ge_five_of_forall_diagonal_lcm_obstruction`. These do not prove
  the diagonal compression; they record the exact proof contract.
- New Lean large-prime diagonal contract:
  `filterMatrixRowsProd_pos`, `filterMatrixColumnsProd_pos`,
  `upperQuotientProd_pos`,
  `no_eq_four_of_large_prime_diagonal_factorization_obstruction`, and
  `no_eq_four_ge_five_of_forall_large_prime_diagonal_factorization_obstruction`.
  The Lean-side matrix bound is now discharged by
  `filterMatrixColumnsProd_factorization_le_delta`; the wrapper
  `no_eq_four_ge_five_of_forall_large_prime_diagonal_obstruction` reduces the
  large-prime route to only the pure-window witness theorem checked by the
  diagonal-prime verifier.
- New Lean local large-prime/cancellation reductions:
  `gcd_factorization_le_left`,
  `gcd_factorization_eq_zero_of_not_dvd_right`,
  `gcdIntervalProd_factorization_eq_zero_of_forall_not_dvd`,
  `gcdIntervalProd_factorization_le_of_length_lt_prime`,
  `shared_prime_power_forces_filterMatrixCell_dvd`, and
  `shared_prime_power_forces_filterMatrixCell_dvd_of_window`. These prove the
  local facts behind the large-prime route: a length-`k` termwise-gcd product
  has at most the left modulus's `p`-valuation for `p>k`, and any shared prime
  power between an upper term and a lower term must divide the corresponding
  exact-filter matrix cell.
- Latest GPT Pro status: do not treat the large-prime surplus theorem as a
  Sylvester-Schur corollary. The missing content is a two-interval primitive
  surplus/cancellation obstruction. GPT Pro gave small-threshold failures for
  the analogous surplus claim at `k=2,d=6,x=7` and `k=3,d=21,x=11`; no `k>=5`
  survivor was found in its additional bounded/random checks, but this remains
  evidence only.
- New Lean lcm-half theorems:
  `filterMatrixRowsProd_eq_filterMatrixColumnsProd`,
  `lcm_interval_upperQuotientProd_dvd_filterMatrixColumnsProd_of_filters`, and
  `lcm_interval_upperQuotientProd_dvd_filterMatrixColumnsProd_of_eq_four_ge_five`.
  Thus the local filters already imply `lcm(P_A,P_Q) | G`, with
  `G = filterMatrixColumnsProd d (n-d) k k`, for any hypothetical remaining
  `N=4,k>=5` solution. The remaining diagonal compression theorem is the
  narrower statement `G | diagonalDelta d k * diagonalH k`; together with the
  pure all-window obstruction, it closes every remaining `N=4,k>=5` row.
- New Lean diagonal bridge theorems:
  `filterMatrixPositiveDiagonalProd`, `filterMatrixNegativeDiagonalProd`,
  `filterMatrixCell_eq_positiveDiagonal`, `filterMatrixCell_eq_negativeDiagonal`,
  `filterMatrixPositiveDiagonalProd_eq_gcdIntervalProd`, and
  `filterMatrixNegativeDiagonalProd_eq_gcdIntervalProd`. These identify each
  diagonal of the matrix as a consecutive termwise-gcd product.
- New Lean per-diagonal large-prime valuation bounds:
  `filterMatrixPositiveDiagonalProd_factorization_le_of_length_lt_prime`,
  `filterMatrixNegativeDiagonalProd_factorization_le_of_length_lt_prime`,
  `filterMatrixPositiveDiagonalProd_factorization_le_of_window`, and
  `filterMatrixNegativeDiagonalProd_factorization_le_of_window`. For `p>k`,
  each diagonal contributes at most the `p`-valuation of its fixed modulus
  `d+h` or `d-h`. The aggregate assembly from these diagonal estimates to
  `v_p(filterMatrixColumnsProd d x k k) <= v_p(diagonalDelta d k)` is now
  Lean-backed.
- New Lean diagonal aggregate definitions and valuation theorem:
  `filterMatrixPositiveDiagonalsProd`, `filterMatrixNegativeDiagonalsProd`,
  `filterMatrixDiagonalsProd`, `positiveDiagonalModuliProd`,
  `negativeDiagonalModuliProd`, `diagonalModuliProd`,
  `filterMatrixPositiveDiagonalsProd_factorization_le_moduli`,
  `filterMatrixNegativeDiagonalsProd_factorization_le_moduli`,
  `filterMatrixDiagonalsProd_factorization_le_moduli`,
  `diagonalModuliProd_eq_diagonalDelta`, and
  `filterMatrixColumnsProd_eq_filterMatrixDiagonalsProd`,
  `filterMatrixDiagonalsProd_factorization_le_delta`, and
  `filterMatrixColumnsProd_factorization_le_delta`. These prove
  `v_p(filterMatrixColumnsProd d x k k) <= v_p(diagonalDelta d k)` for
  `p>k`.
- New Lean shifted-quotient packaging:
  `diagonalH_pos`, `diagonalH_not_dvd_of_lt`,
  `diagonalH_factorization_eq_zero_of_lt`,
  `diagonal_lcm_obstruction_of_shifted_sylvester_quotient`, and
  `no_eq_four_ge_five_of_forall_shifted_sylvester_quotient`. These formalize
  the GPT Pro observation that a prime `p>k` with
  `v_p(lcm(P_A,P_Q)) > v_p(Delta_k(d))` gives
  `lcm(P_A,P_Q) ∤ Delta_k(d)*H_k`, because `H_k` has no prime factors above
  `k`. This is a conditional route only: the shifted quotient theorem and the
  full compression theorem `G | Delta_k(d)*H_k` are not proved.
- `scripts/verify_erdos686_diagonal_lcm.py`: bounded verifier for the diagonal
  pure-window obstruction
  `lcm(P_A,P_Q) ∤ Delta_k(d)*H_k`.
- `examples/erdos-problems/attack/erdos686-diagonal-lcm-prefix-k24-d299-x299-draft.v1.json`:
  verified artifact for `5<=k<=24`, `k<=d<=299`, `k<x<=299`, checking
  `1,625,160` states. It has `first_survivor = null`; the first checked state
  `k=5,d=5,x=6,n=11` already violates the necessary divisibility.
- `scripts/verify_erdos686_diagonal_prime_witness.py`: bounded verifier for the
  stronger large-prime form of the diagonal obstruction. It looks for `p>k`
  with `max(v_p(P_A),v_p(P_Q)) > v_p(Delta_k(d))`, which is enough because
  `H_k` has no prime factors above `k`.
- `examples/erdos-problems/attack/erdos686-diagonal-prime-witness-prefix-k24-d299-x299-draft.v1.json`:
  verified artifact for the same prefix. It checks `1,625,160` states with
  `first_survivor = null`; the first checked state is witnessed by `p=13` on
  the lower block.
- New Lean novel-large-prime diagonal obstruction:
  `upperQuotientProd_term_dvd`,
  `large_prime_diagonal_obstruction_of_dvd_not_dvd_delta`,
  `no_eq_four_ge_five_of_forall_novel_large_prime_diagonal_obstruction`, and
  `no_eq_four_ge_five_of_forall_novel_large_prime_term_obstruction`. These
  sharpen the remaining pure input: it is enough to find a prime `p>k` in one
  lower-block term `d+x+j` or one upper-quotient term
  `(2*d+x+s)/gcd(2*d+x+s,4)` which does not divide `Delta_k(d)` at all.
- New Lean captured-prime reduction:
  `lower_captured_prime_le_gap`, `upper_captured_prime_le_gap`,
  `lower_captured_prime_dvd_difference_window`, and
  `upper_captured_prime_dvd_difference_window`. If a lower-block prime is
  absorbed by `Delta_k(d)`, it divides `(x-k+2)...(x+2k-1)` and is at most
  `x+2k-1`. If an upper-quotient prime is absorbed by `Delta_k(d)`, it divides
  `(d+x-k+2)...(d+x+2k-1)` and is at most `d+x+2k-1`. This turns the remaining
  pure theorem into a comparison of explicit consecutive blocks.
- The broad difference-window obstruction is false as a standalone target:
  at `k=5,d=5,x=6,n=11`, the large primes in the lower and upper blocks are
  present in the overlarge difference windows even though `p=13` is novel
  relative to `Delta_5(5)`.
- New exact residue-level Lean closure:
  `lower_novel_of_not_dvd_difference_window`,
  `upper_novel_of_not_dvd_difference_window`,
  `no_eq_four_ge_five_of_forall_difference_window_obstruction`,
  `lower_term_novel_of_missing_delta_residue`,
  `upper_term_novel_of_missing_delta_residue`, and
  `no_eq_four_ge_five_of_forall_novel_large_prime_residue_obstruction`. The
  useful remaining target is residue-level: find a prime in one specific lower
  or upper term that misses every matching diagonal residue, not merely the
  whole overlarge difference window.
- New finite-prefix Lean contract:
  `no_eq_four_ge_five_of_forall_finite_prefix_residue_obstruction`. The
  current candidate theorem is that lower terms `j<=14` or upper quotient terms
  `s<=5` always suffice for the residue-level obstruction.
- `examples/erdos-problems/attack/erdos686-diagonal-novel-prime-j14-s5-prefix-k24-d700-x700-draft.v1.json`:
  verified artifact for the finite-prefix version over `5<=k<=24`,
  `k<=d<=700`, `k<x<=700`, checking `9,412,580` states with
  `first_survivor = null`. A separate 20,000-state random/adversarial stress
  test outside the rectangle found no failure, but that is heuristic only.
- New prefix-grid verifier mode in `scripts/verify_erdos686_diagonal_novel_prime.py`.
  Artifact
  `examples/erdos-problems/attack/erdos686-diagonal-novel-prime-prefix-grid-k24-d700-x700-draft.v1.json`
  checks all `0<=J<=15`, `0<=S<=7` over the same rectangle. In that bounded
  grid, `[14,5]` is the unique Pareto-minimal passing pair. Exact adjacent
  failures: `[13,5]` fails at `k=14,d=54,x=59,n=113` with first lower witness
  `j=14` and first upper witness `s=6`; `[14,4]` fails at
  `k=5,d=15,x=32,n=47` with no lower witness and first upper witness `s=5`.
- New `k_min` verifier scope plus high-`k` lower-only tail artifact:
  `examples/erdos-problems/attack/erdos686-diagonal-novel-prime-lower-j14-tail-k14-k30-d900-x900-draft.v1.json`.
  It checks the named split candidate `14<=k<=30`, `k<=d<=900`, `k<x<=900`,
  lower prefix `j<=14`, upper prefix `0`, with `13,120,362` states and
  `first_survivor = null`. In the original `k<=24,d,x<=700` rectangle, all
  upper-rescue rows occur only at `k in {5,6,7,8,9,12,13}`. This suggests
  splitting the global theorem into a high-`k` lower-only lemma and finitely
  many low-`k` upper-rescue lemmas; it is still bounded evidence only.
- `scripts/verify_erdos686_diagonal_novel_prime.py`: bounded verifier for the
  novel-prime variant of the diagonal obstruction.
- `examples/erdos-problems/attack/erdos686-diagonal-novel-prime-prefix-k24-d700-x700-draft.v1.json`:
  verified artifact for `5<=k<=24`, `k<=d<=700`, `k<x<=700`, checking
  `9,412,580` states. It has `first_survivor = null`; the first witness is
  `p=13` on the lower block at `k=5,d=5,x=6,n=11`. The witness source
  histogram is `lower = 9,412,435`, `upper_quotient = 145`, suggesting the
  lower block may carry the main theorem while the upper quotient catches rare
  edge states.
- Natural lower-only variant is false: at `k=5,d=9,x=14,n=23`, lower block
  `24..28` has large primes `{7,13}`, both already in `Delta_5(9)`, while the
  upper quotient supplies novel primes `17` and `37`.
- Latest GPT Pro two-capture reduction is now partly Lean-backed. New
  definition `lowerCaptured k d t` means every prime `p>k` dividing the shifted
  lower block `P_k(d+t)` already divides `Delta_k(d)`. New theorem
  `no_eq_four_ge_five_of_forall_no_two_lowerCaptured` proves that a global
  theorem
  `¬ (lowerCaptured k d x ∧ lowerCaptured k d (d+x))` for all
  `5<=k`, `k<=d`, `k<x` would close every remaining `N=4,k>=5` row through the
  existing novel-prime diagonal obstruction. This is only a reduction; the
  no-two-captures theorem is open.
- New Lean quotient helpers:
  `prime_dvd_div_gcd_four_of_ne_two` and
  `prime_dvd_upperQuotientProd_of_dvd_raw_interval`. They formalize that for
  odd primes, removing the `gcd(_,4)` factor from an upper term does not remove
  prime divisibility, so the upper raw block `P_k(2d+x)` can be treated as the
  shifted lower block at `d+x` for the capture reduction.
- New Lean doubled-residue upper lemmas:
  `upper_raw_captured_prime_dvd_double_difference` and
  `upper_quotient_term_novel_of_missing_double_difference`. If a raw upper term
  is captured by `Delta_k(d)`, then it has a residue
  `x+s+2k-2r` for some `r in [1,2k-1]`, equivalently the centered form
  `x+s-2h`. An upper quotient term missing every such doubled residue is novel
  relative to `Delta_k(d)`.
- New no-two-captures verifier and artifact:
  `scripts/verify_erdos686_no_two_lower_captured.py` and
  `examples/erdos-problems/attack/erdos686-no-two-lower-captured-prefix-k24-d700-x700-draft.v1.json`.
  The artifact replays cleanly over `5<=k<=24`, `k<=d<=700`, `k<x<=700`,
  checking `9,412,580` states with `first_survivor = null`. It tests exactly
  the Lean-backed target
  `¬ (lowerCaptured k d x ∧ lowerCaptured k d (d+x))`; the first witness is
  `k=5,d=5,x=6,n=11`, `p=13` in the lower `x` block. Source histogram:
  `lower_x = 9,412,435`, `shifted_d_plus_x = 145`. This is bounded evidence,
  not a proof.
- New profile mode in the same verifier:
  `examples/erdos-problems/attack/erdos686-lower-captured-profile-k24-d2000-x2000-draft.v1.json`.
  It replays over `5<=k<=24`, `k<=d<=2000`, `k<x<=2000`, checking
  `78,884,580` states. It records exactly the checked rows where
  `lowerCaptured k d x` holds: `152` rows, only for
  `k in {5,6,7,8,9,12,13}`. Every listed row has a shifted uncaptured witness,
  always with shifted term index `j<=5`. This supports the candidate split
  `k>=14` lower-only plus low-`k` shifted-rescue, but it is still bounded
  theorem-mining evidence only.
- New Lean split contract:
  `no_eq_four_ge_five_of_forall_lowerCaptured_split`. It closes all remaining
  `N=4,k>=5` rows from two external inputs matching the profile evidence:
  `k>=14 -> ¬ lowerCaptured k d x`, and for `5<=k<14`, every
  `lowerCaptured k d x` row has an uncaptured prime in one of the first five
  shifted terms `2*d+x+j`. This is a conditional wrapper only.
- Latest GPT Pro boundary formulation has a concrete checker:
  `scripts/verify_erdos686_boundary_absorption.py`. It tests the named
  obstruction that no state can have every lower/upper `p>2k` rough part
  absorbed by valid exact-filter cells times boundary fake-cancellation
  factors. Artifacts:
  `examples/erdos-problems/attack/erdos686-boundary-absorption-prefix-k24-d299-x299-draft.v1.json`
  and
  `examples/erdos-problems/attack/erdos686-boundary-absorption-prefix-k24-d700-x700-draft.v1.json`.
  The larger artifact replays cleanly over `5<=k<=24`, `k<=d<=700`,
  `k<x<=700`, checking `9,412,580` states with `first_survivor = null`.
  First failure: `k=5,d=5,x=6`, lower term `13`, rough part `13`,
  `valid_cell_product=1`, `boundary_product=5280`, and `5280 mod 13 = 2`.
  First-failure histogram: `lower=9,412,466`, `upper=114`. This is bounded
  theorem-mining evidence only, not a finite reduction or proof.
- New GPT Pro sharpening: frame the remaining shifted quotient theorem as
  rough-lcm nondivisibility. For
  `A=intervalProd (d+x) k`, `Q=upperQuotientProd (d+x) d k`, and
  `Delta=diagonalDelta d k`, the target is a prime `p>k` with
  `v_p(lcm(A,Q)) > v_p(Delta)`, equivalently
  `¬ roughPartAbove k (lcm A Q) | Delta`. Under no surplus, every term's
  large rough part must be absorbed by diagonal gcds; for `p>2k` this splits
  into valid exact-filter cells plus boundary fake-cancellation factors. The
  rough-lcm packaging is now Lean-backed by `roughPartAbove`,
  `roughPartAbove_factorization`,
  `prime_dvd_roughPartAbove_of_dvd`,
  `roughPartAbove_dvd_iff_no_large_factorization_surplus`,
  `exists_large_factorization_surplus_iff_not_roughPartAbove_dvd`,
  `roughPartAbove_lcm_factorization`,
  `exists_large_lcm_max_surplus_iff_not_roughPartAbove_lcm_dvd`,
  `shifted_quotient_padic_iff_rough_lcm_not_dvd`,
  `no_eq_four_ge_five_of_forall_rough_lcm_not_dvd`,
  `shifted_quotient_max_padic_iff_rough_lcm_not_dvd`, and
  `no_eq_four_ge_five_of_forall_shifted_quotient_max_surplus`. The next pass
  also Lean-backed the termwise rough-part precursor:
  `no_large_prime_surplus_forces_lower_term_roughPart_dvd_delta` and
  `no_large_prime_surplus_forces_upper_quotient_term_roughPart_dvd_delta`.
  The following pass Lean-backed the full-diagonal absorption products:
  `lowerDeltaAbsorbProd`, `upperDeltaAbsorbProd`,
  `gcdIntervalProd_dvd_lowerDeltaAbsorbProd`,
  `gcdIntervalProd_dvd_upperDeltaAbsorbProd`,
  `no_large_prime_surplus_forces_lower_term_roughPart_dvd_lowerDeltaAbsorbProd`,
  and
  `no_large_prime_surplus_forces_upper_quotient_term_roughPart_dvd_upperDeltaAbsorbProd`.
  The next pass Lean-backed the valid/boundary product split:
  `lowerValidCellProd`, `lowerBoundaryAbsorbProd`, `upperValidCellProd`,
  `upperBoundaryAbsorbProd`,
  `gcdIntervalProd_dvd_lowerValidCellProd_mul_lowerBoundaryAbsorbProd`,
  `gcdIntervalProd_dvd_upperValidCellProd_mul_upperBoundaryAbsorbProd`,
  `no_large_prime_surplus_forces_lower_term_roughPart_dvd_validCell_mul_boundary`,
  and
  `no_large_prime_surplus_forces_upper_quotient_term_roughPart_dvd_validCell_mul_boundary`.
  The next pass Lean-backed block-valuation localization:
  `intervalProd_factorization_eq_zero_of_forall_not_dvd`,
  `intervalProd_factorization_eq_term_of_dvd_length_lt`,
  `upperQuotientProd_pos_all`,
  `upperQuotientProd_factorization_eq_zero_of_forall_not_dvd`, and
  `upperQuotientProd_factorization_eq_term_of_dvd_length_lt`.
  A large modulus `p>k` dividing a chosen lower or upper quotient term
  carries the whole corresponding block-product valuation.
  Thus both the lcm-valuation and exact `max(v_p(A),v_p(Q))` formulations are
  packaged, and no-surplus is known to force every lower/upper quotient term's
  `p>k` rough part into `Delta_k(d)` and then into the full lower/upper
  diagonal difference products, then into valid matrix cells times boundary
  fake-cancellation products. The latest Lean pass packages the two per-term
  valid/boundary statements as
  `no_large_prime_surplus_forces_termwise_valid_boundary_absorption`, and also
  adds `roughPartAbove_dvd_roughPartAbove_of_le` plus the `p>2k` corollary
  `no_large_prime_surplus_forces_termwise_valid_boundary_absorption_above_two_mul`.
  The next pass added
  `diagonalDelta_factorization_eq_term_of_dvd_above_two_mul`,
  `no_large_prime_surplus_forces_lower_validCell_factorization_ge_term`, and
  `no_large_prime_surplus_forces_upper_validCell_factorization_ge_quotient_term`.
  Thus valid diagonal slots now carry the captured lower/upper quotient
  term's full `p`-valuation above `2k`.
  The next pass added row-column forms
  `no_large_prime_surplus_forces_lower_cell_factorization_ge_term`,
  `no_large_prime_surplus_forces_upper_cell_factorization_ge_quotient_term`,
  and the paired max-control wrapper
  `no_large_prime_surplus_forces_validCell_factorization_ge_pair`.
  The next pass added
  `lowerValidCellProd_eq_filterMatrixColumnProd`,
  `upperValidCellProd_eq_filterMatrixRowProd`,
  `filterMatrixRowProd_dvd_filterMatrixRowsProd`,
  `filterMatrixRowProd_dvd_filterMatrixColumnsProd`,
  `lowerValidCellProd_dvd_filterMatrixColumnsProd`,
  `upperValidCellProd_dvd_filterMatrixColumnsProd`,
  `filterMatrixCell_dvd_filterMatrixColumnProd`,
  `filterMatrixColumnProd_dvd_filterMatrixColumnsProd`,
  `filterMatrixCell_dvd_filterMatrixColumnsProd`, and the full-matrix lift
  `no_large_prime_surplus_forces_filterMatrixColumnsProd_factorization_ge_pair`.
  The next pass added matrix-boundary wrappers
  `boundaryAbsorbedState_lower_roughPart_dvd_matrix_mul_boundary`,
  `boundaryAbsorbedState_upper_roughPart_dvd_matrix_mul_boundary`,
  `shifted_quotient_max_surplus_of_lower_matrix_boundary_failure`, and
  `shifted_quotient_max_surplus_of_upper_matrix_boundary_failure`, so valid
  cells can now be absorbed into the full matrix mass before checking the
  residual boundary product.
  The next pass added aggregate products
  `lowerRoughPartProd`, `upperRoughPartProd`,
  `lowerBoundaryAbsorbRowsProd`, and `upperBoundaryAbsorbRowsProd`, then proved
  `boundaryAbsorbedState_lowerRoughPartProd_dvd_matrix_pow_mul_lowerBoundary`,
  `boundaryAbsorbedState_upperRoughPartProd_dvd_matrix_pow_mul_upperBoundary`,
  `shifted_quotient_max_surplus_of_lower_aggregate_matrix_boundary_failure`,
  and `shifted_quotient_max_surplus_of_upper_aggregate_matrix_boundary_failure`.
  This converts the residual check into a product-level nondivisibility option:
  rough-term product versus matrix-power times aggregate boundary product.
  The next pass added `roughPartAbove_one`, `roughPartAbove_mul`,
  `roughPartAbove_intervalProd`, `roughPartAbove_upperQuotientProd`,
  `lowerRoughPartProd_eq_roughPartAbove_intervalProd`,
  `upperRoughPartProd_eq_roughPartAbove_upperQuotientProd`,
  `shifted_quotient_max_surplus_of_lower_block_matrix_boundary_failure`,
  `shifted_quotient_max_surplus_of_upper_block_matrix_boundary_failure`,
  `no_eq_four_ge_five_of_forall_lower_block_matrix_boundary_failure`, and
  `no_eq_four_ge_five_of_forall_upper_block_matrix_boundary_failure`.
  Thus the aggregate obstruction can now be stated directly using the original
  lower interval rough part or upper quotient rough part. This aggregate target
  is now known false as a universal theorem:
  `scripts/verify_erdos686_aggregate_boundary.py` and
  `examples/erdos-problems/attack/erdos686-aggregate-boundary-prefix-k10-d80-x80-draft.v1.json`
  show the first checked state `k=5,d=5,x=6,n=11` has both aggregate
  divisibilities true (`lower_rough_part=13`, `upper_rough_part=323`, and
  `upper_boundary_product=0`). The missing condition is row-locality; aggregate
  products allow cross-row and zero-boundary absorption that the local
  `boundaryAbsorbedState` predicate was designed to prevent.
  Lean now records the same aggregate-route failure as
  `aggregate_matrix_boundary_disjunction_counterexample` and
  `not_forall_lower_or_upper_block_matrix_boundary_failure`, so the disjunctive
  aggregate theorem is kernel-refuted at `k=5,d=5,x=6`.
  The next pass named the exact checker predicate as `boundaryAbsorbedState`
  and added `no_large_prime_surplus_forces_boundaryAbsorbedState`,
  `shifted_quotient_max_surplus_of_not_boundaryAbsorbedState`,
  `shifted_quotient_max_surplus_of_lower_boundary_failure`,
  `shifted_quotient_max_surplus_of_upper_boundary_failure`, and
  `no_eq_four_ge_five_of_forall_not_boundaryAbsorbedState`.
  Thus the Python checker's first lower/upper boundary failure payload now has
  a direct Lean bridge to a max-valuation surplus witness.
  The latest Lean pass added the first row-local lower nondivisibility bridge:
  `lowerBoundaryAbsorbProdAux_pos`,
  `lowerBoundaryAbsorbProdAux_factorization_eq_zero_of_term_dvd_forall_not_dvd_diagonal`,
  `lowerBoundaryAbsorbProd_factorization_eq_zero_of_term_dvd_forall_not_dvd_diagonal`,
  `lowerValidCellProd_pos`,
  `lowerValidCellProd_factorization_eq_zero_of_forall_not_dvd_diagonal`,
  `lowerValidCellProd_mul_lowerBoundaryAbsorbProd_factorization_eq_zero`, and
  `lower_boundary_failure_of_term_prime_missing_diagonal`. If a prime `p>2*k`
  divides a lower term `d+x+j` and misses every centered diagonal modulus
  `d-k+r`, the local lower valid-cell times boundary product has zero
  `p`-valuation, so the row fails `boundaryAbsorbedState`. This banks a
  Lean-checked interface for any lower novel-prime lemma; it is not the global
  `¬ boundaryAbsorbedState` theorem.
  The follow-up Lean pass added
  `shifted_quotient_max_surplus_of_lower_term_prime_missing_diagonal` and
  `no_eq_four_ge_five_of_forall_lower_term_prime_missing_diagonal`, but the
  corresponding lower-only global theorem is false. The artifact
  `examples/erdos-problems/attack/erdos686-lower-term-prime-missing-diagonal-prefix-k24-d700-x700-draft.v1.json`
  replays with
  `python3 scripts/verify_erdos686_diagonal_novel_prime.py --verify ...` and
  finds the first lower-only survivor after `2,789` states:
  `k=5,d=9,x=14,n=23`. There the lower block `24..28` has no prime `p>10`
  missing the centered diagonal window. Keep the local Lean wrapper, but do not
  pursue the lower-only theorem as a global solve route; the target must allow
  upper witnesses or prove the full `¬ boundaryAbsorbedState` theorem.
  The upper row-local companion is now Lean-backed in the correct
  doubled-difference coordinates. New theorem names:
  `upperValidCellProd_factorization_eq_zero_of_forall_not_dvd_double_difference`,
  `upperBoundaryAbsorbProdAux_ne_zero_of_forall_not_dvd_double_difference`,
  `upperBoundaryAbsorbProd_factorization_eq_zero_of_forall_not_dvd_double_difference`,
  `upperValidCellProd_mul_upperBoundaryAbsorbProd_factorization_eq_zero_of_missing_double_difference`,
  `upper_boundary_failure_of_quotient_prime_missing_double_difference`,
  `shifted_quotient_max_surplus_of_upper_quotient_term_prime_missing_double_difference`,
  and
  `no_eq_four_ge_five_of_forall_upper_quotient_term_prime_missing_double_difference`.
  The doubled-difference hypothesis is necessary: a weaker upper
  diagonal-miss theorem can fail because upper boundary factors can be zero.
  This is still a conditional bridge, not the global upper witness theorem.
  The combined row-local target is now named too:
  `no_eq_four_ge_five_of_forall_lower_or_upper_row_local_witness`. It closes the
  remaining `N=4,k>=5` rows if every window has either a lower prime
  `p>2*k` in `d+x+j` missing every `d-k+r`, or an upper quotient prime
  `p>2*k` missing every doubled residue `x+s+2*k-2*r`. This is the realistic
  boundary-route witness theorem after the lower-only target failed; the
  disjunctive arithmetic theorem is still open.
  New verifier:
  `scripts/verify_erdos686_row_local_witness.py`. Artifact
  `examples/erdos-problems/attack/erdos686-row-local-witness-prefix-k24-d700-x700-draft.v1.json`
  replays cleanly over `5<=k<=24`, `k<=d<=700`, `k<x<=700`, checking
  `9,412,580` states with `first_survivor = null`. The source histogram is
  `lower = 9,412,435`, `upper_quotient = 145`. This is bounded evidence for
  the named mixed theorem, not a finite reduction or proof.
  The same verifier now has a mixed prefix-grid artifact:
  `examples/erdos-problems/attack/erdos686-row-local-witness-prefix-grid-k24-d700-x700-draft.v1.json`.
  It checks all `0<=J<=15`, `0<=S<=7` over the same rectangle. `[14,5]` is the
  unique Pareto-minimal passing pair. Adjacent failures:
  `[13,5]` fails at `k=14,d=54,x=59,n=113` with `first_lower_j=14`,
  `first_upper_s=6`; `[14,4]` fails at `k=5,d=15,x=32,n=47` with no lower
  witness and `first_upper_s=5`. This is bounded split evidence only.
  Lean now records the matching prefix-contract wrapper
  `no_eq_four_ge_five_of_forall_lower14_or_upper5_row_local_witness`: a global
  proof of the `[14,5]` row-local witness theorem would close all remaining
  `N=4,k>=5` rows. The constants are evidence-backed only, not proved.
  The aggregate matrix-to-diagonal side is already Lean-backed too. Still open:
  proving `∀ d x k, 5 ≤ k → k ≤ d → k < x → ¬ boundaryAbsorbedState d x k`.
- Follow-up prompt:
  `docs/erdos-attack/686/gpt-pro-diagonal-novel-prime-prompt.md`.
- Older broader diagonal prompt:
  `docs/erdos-attack/686/gpt-pro-diagonal-lcm-obstruction-prompt.md`.
- `intervalProd_mul_square_of_eq_four`: if
  `intervalProd (n+d) k = 4*intervalProd n k`, then the product of the two
  interval blocks is a square.
- `intervalProd_factorization_eq_of_odd_prime`: every odd-prime valuation
  agrees across an `N=4` interval equation.
- `intervalProd_factorization_two_eq_add_two`: the `2`-adic valuation increases
  by exactly `2` across an `N=4` interval equation.

Draft record:

- `examples/erdos-problems/attack/erdos686-k2-k4-prime-power-square-obstruction-record-draft.v1.json`

Prefix evidence:

- `scripts/verify_erdos686_k3_n4_prefix.py`
- `examples/erdos-problems/attack/erdos686-k3-n4-prefix-20m-draft.v1.json`
- Checks every `2<=Y<=20000000` in
  `X^3-X=4(Y^3-Y)`, where `X=m+2`, `Y=n+2`.
- Finds no admissible solution; the only exact hit is `(X,Y)=(3,2)`, giving
  nonadmissible `(m,n)=(1,0)`.
- This is not a proof of the `k=3` case without a proved upper bound on `Y`.

Conditional and elementary certificate verifier:

- `scripts/verify_erdos686_n4_k3_k6_k8.py`
- `examples/erdos-problems/attack/erdos686-n4-k3-k6-k8-gptpro-certificate-draft.v1.json`
- `k=3`: after the standard reduction to `C=4u^3-v^3`, the script verifies the
  finite certificate `1<=u<58000`, `C|60`, `v^3=4u^3-C`, `C*D^2=4u-v`.
  It finds only `(u,v,C,D)=(1,1,3,1)` and `(2,3,5,1)`, corresponding to
  `(m,n)=(-1,-1)` and nonadmissible `(m,n)=(1,0)`. This row is conditional on
  the external irrationality estimate
  `|cuberoot(4)-v/u| > 0.072*u^(-2.45)`.
- `k=6`: verifies
  `A(x)^2 = 256*P_6(x)+9*(84*x^2+588*x+1129)`, the exact tail inequalities
  from `n>=200`, the monotone cutoff `P_6(261)>4*P_6(199)`, and no hits in
  `0<=n<200`, `n+6<=m<=260`. The square identity, oddness, residual positivity,
  residual tail inequalities including `2*S(x)<A(x)`, half-step tail
  contradiction, finite box, and monotone small-`n` cutoff are now
  Lean-kernelized. The combined theorem `no_k6_four` rules out all admissible
  `N=4,k=6` rows.
- `k=8`: verifies
  `Q(x)^2 = P_8(x)+16*(2*x+9)^2`, the exact tail inequalities from `n>=20`,
  the parity contradiction after forcing `Q(m)=2Q(n)`, the monotone cutoff
  `P_8(35)>4*P_8(19)`, and no hits in `0<=n<20`, `n+8<=m<=34`. The square
  identity, evenness, residual positivity, residual tail inequalities, cutoff,
  approximation-forcing step, final tail contradiction, finite box, and
  monotone small-`n` cutoff are now Lean-kernelized. The combined theorem
  `no_k8_four` rules out all admissible `N=4,k=8` rows.
- `k=10`: `scripts/verify_erdos686_k10_n4.py` gives a script-backed exact
  local certificate. It proves the centered tail from `y>=81127`
  (`n>=40558`) by polynomial coefficient positivity and checks the remaining
  finite box `0<=n<40558`, `m>=n+10` by one exact binary search per `n`, with
  `0` hits and finite-box cutoff `m<=46588`. Artifact:
  `examples/erdos-problems/attack/erdos686-n4-k10-certificate-draft.v1.json`.
- `k=12`: `scripts/verify_erdos686_k12_n4.py` gives a script-backed exact
  local certificate. It proves the centered tail from `y>=1493` (`n>=740`) by
  polynomial coefficient positivity, forces the residual equations, kills them
  by a modulo `13` obstruction, and checks the remaining finite box
  `0<=n<740`, `m>=n+12` by one exact binary search per `n`, with `0` hits and
  finite-box cutoff `m<=830`. Artifact:
  `examples/erdos-problems/attack/erdos686-n4-k12-certificate-draft.v1.json`.
- `k=14`: `scripts/verify_erdos686_k14_n4.py` gives a script-backed exact
  local certificate. It proves the centered tail from `y>=82427960`
  (`n>=41213973`) by polynomial coefficient positivity and clears denominators
  with `D=16`; the forced equality `H(Y_m)=2H(Y_n)` contradicts odd/even parity
  after multiplying by `D`. It checks the remaining finite box
  `0<=n<41213973`, `m>=n+14` by exact monotone recurrence, with `0` hits and
  largest minimal candidate `m=45503916`. Artifact:
  `examples/erdos-problems/attack/erdos686-n4-k14-certificate-draft.v1.json`.
- `k=16`: `scripts/verify_erdos686_k16_n4.py` gives a script-backed exact
  local certificate. It proves the centered tail from `y>=46251` (`n>=23117`)
  by polynomial coefficient positivity, forces the residual equations, kills
  them by a modulo `17` obstruction, and checks the remaining finite box
  `0<=n<23117`, `m>=n+16` by one exact binary search per `n`, with `0` hits and
  finite-box cutoff `m<=25208`. Artifact:
  `examples/erdos-problems/attack/erdos686-n4-k16-certificate-draft.v1.json`.
- `k=20`: `scripts/verify_erdos686_k20_n4.py` gives a script-backed exact
  local certificate. It proves the centered tail from `y>=1796958`
  (`n>=898469`) by polynomial coefficient positivity, forces the residual
  equations, kills them by a modulo `9` obstruction, and checks the remaining
  finite box `0<=n<898469`, `m>=n+20` by exact monotone recurrence, with `0`
  hits and largest minimal candidate `m=962955`. Artifact:
  `examples/erdos-problems/attack/erdos686-n4-k20-certificate-draft.v1.json`.
- `k=24`: `scripts/verify_erdos686_k24_n4.py` gives a script-backed exact
  local certificate. It proves the centered tail from `y>=84007164`
  (`n>=42003570`) by polynomial coefficient positivity, forces the residual
  equations, kills them by a modulo `25` obstruction, and checks the remaining
  finite box `0<=n<42003570`, `m>=n+24` by exact monotone recurrence, with `0`
  hits and largest minimal candidate `m=44501232`. Artifact:
  `examples/erdos-problems/attack/erdos686-n4-k24-certificate-draft.v1.json`.
- `k=18` barrier: the dyadic lattice obstruction exists, but the direct tail
  starts at `Y=612477216640` (`n>=306238608311`), so this route needs a sharper
  equality-compatible tail bound or a different finite reduction.
- Even-`k` method profile:
  `scripts/verify_erdos686_even_square_method.py` and
  `examples/erdos-problems/attack/erdos686-even-square-method-r80-draft.v1.json`.
  For `k=2r`, it computes
  `F_r(y)=prod_j(y^2-(2j-1)^2)=2^(2r)P_{2r}(x)`, the formal square-root part
  `H_r`, and the residual `R_r=H_r^2-F_r`. It verifies through `r<=80` that
  odd `r` rows have the dyadic lattice obstruction and even `r` rows have a
  modular obstruction after the forced residual equations. The first failure of
  the tempting "modulus <=100 always suffices" shortcut is `r=16`, `k=32`,
  needing modulus `121`; the largest first modulus found through `r<=80` is
  `1531` at `k=160`. This is only a bounded profile for a possible even-`k`
  certificate generator. The verifier now also records the proof-shaped odd-`r`
  denominator pattern `denom_lcm(H_r)=2^(s+v2(s!))` for `r=2s+1`, with only the
  linear coefficient odd after clearing denominators. It also records that the
  crude odd-tail inequality has crossover scale `5*D*lead(R_r)`, already
  `405625` at `r=5` and `1648559185/4` at `r=7`, so the missing input is a
  sharper equality-compatible tail bound. The verifier's replay mode
  recomputes all rows from the artifact parameters and currently returns
  `errors: []`.
- Current covered local rows for `N=4`: `k=2,3,4,6,8,10,12,14,16,20,24`, with only
  `k=2,4,6,8` fully Lean-kernelized, `k=3` conditional on the external
  irrationality estimate, and `k=10,12,14,16,20,24` script-backed. The all-`k`
  Lean filters `shift_dvd_three_mul_intervalProd` and
  `intervalProd_local_shift_dvd`, the large-prime lower-block obstructions,
  the near-diagonal contradiction, the prime-free lower-block reduction, the
  Bertrand-backed `k<n` bound, the sparse-window `2*k<n` bound, the
  near-diagonal `d+k<n` bound, the far-shift `d<n+k` bound, plus the bundled
  remaining-window, aggregate-divisibility, signed gap-product transfer,
  smaller-product divisor, square-product, and valuation theorems and the
  per-factor smoothness filters and matrix large-prime uniqueness, are proof
  infrastructure only. The dual-tail smoothness filter alone is explicitly falsified by
  `scripts/verify_erdos686_dual_tail_counterexample.py`.

Verified:

```bash
cd lean && lake env lean Vela/Erdos686.lean
cd lean && lake build Vela.Erdos686
python3 -m py_compile scripts/verify_erdos686_n4_k3_k6_k8.py
python3 scripts/verify_erdos686_n4_k3_k6_k8.py --out examples/erdos-problems/attack/erdos686-n4-k3-k6-k8-gptpro-certificate-draft.v1.json
python3 -m py_compile scripts/verify_erdos686_even_square_method.py
python3 scripts/verify_erdos686_even_square_method.py --max-r 80 --small-mod-bound 100 --max-modulus 2000 --out examples/erdos-problems/attack/erdos686-even-square-method-r80-draft.v1.json
python3 scripts/verify_erdos686_even_square_method.py --verify examples/erdos-problems/attack/erdos686-even-square-method-r80-draft.v1.json
python3 scripts/verify_erdos686_k10_n4.py --out examples/erdos-problems/attack/erdos686-n4-k10-certificate-draft.v1.json
python3 scripts/verify_erdos686_k10_n4.py --verify examples/erdos-problems/attack/erdos686-n4-k10-certificate-draft.v1.json
python3 scripts/verify_erdos686_k12_n4.py --out examples/erdos-problems/attack/erdos686-n4-k12-certificate-draft.v1.json
python3 scripts/verify_erdos686_k12_n4.py --verify examples/erdos-problems/attack/erdos686-n4-k12-certificate-draft.v1.json
python3 scripts/verify_erdos686_k14_n4.py --out examples/erdos-problems/attack/erdos686-n4-k14-certificate-draft.v1.json
python3 scripts/verify_erdos686_k14_n4.py --verify examples/erdos-problems/attack/erdos686-n4-k14-certificate-draft.v1.json
python3 scripts/verify_erdos686_k16_n4.py --out examples/erdos-problems/attack/erdos686-n4-k16-certificate-draft.v1.json
python3 scripts/verify_erdos686_k16_n4.py --verify examples/erdos-problems/attack/erdos686-n4-k16-certificate-draft.v1.json
python3 scripts/verify_erdos686_k20_n4.py --out examples/erdos-problems/attack/erdos686-n4-k20-certificate-draft.v1.json
python3 scripts/verify_erdos686_k20_n4.py --verify examples/erdos-problems/attack/erdos686-n4-k20-certificate-draft.v1.json
python3 scripts/verify_erdos686_k24_n4.py --out examples/erdos-problems/attack/erdos686-n4-k24-certificate-draft.v1.json
python3 scripts/verify_erdos686_k24_n4.py --verify examples/erdos-problems/attack/erdos686-n4-k24-certificate-draft.v1.json
python3 -m py_compile scripts/verify_erdos686_factorial_ratio.py
python3 scripts/verify_erdos686_factorial_ratio.py --verify examples/erdos-problems/attack/erdos686-factorial-ratio-prefix-k24-d299-x299-draft.v1.json
python3 -m py_compile scripts/verify_erdos686_large_prime_surplus.py
python3 scripts/verify_erdos686_large_prime_surplus.py --verify examples/erdos-problems/attack/erdos686-large-prime-surplus-prefix-k24-d299-x299-draft.v1.json
python3 -m py_compile scripts/verify_erdos686_diagonal_lcm.py
python3 scripts/verify_erdos686_diagonal_lcm.py --verify examples/erdos-problems/attack/erdos686-diagonal-lcm-prefix-k24-d299-x299-draft.v1.json
python3 -m py_compile scripts/verify_erdos686_row_local_witness.py
python3 scripts/verify_erdos686_row_local_witness.py --verify examples/erdos-problems/attack/erdos686-row-local-witness-prefix-k24-d700-x700-draft.v1.json
python3 scripts/verify_erdos686_row_local_witness.py --verify examples/erdos-problems/attack/erdos686-row-local-witness-prefix-grid-k24-d700-x700-draft.v1.json
```

Caveat:

This is not a #686 solve. The remaining rows include every odd `k>=5`, the even
row `k=18`, the even row `k=22`, and every even `k>=26`. A full disproof via
`N=4` still needs an all-`k` obstruction, an all-even certificate generator plus
an odd-`k` obstruction, or a theorem reducing the remaining higher `k` to the
two-factor case.

## #319 status

Triage only. The official problem page asks for the exact extremal size of the
largest minimal signed zero-sum subset, and the formal-conjectures main theorem
is an `IsGreatest` statement for each `N`. The `Theta(N)` observation follows
from the trivial upper bound `c(N)<=N` and the known Croot/Adenwalla lower bound
`c(N)>=(1-1/e+o(1))N`, but it does not answer the official exact question.

Draft record:

- `examples/erdos-problems/attack/erdos319-official-exact-triage-record-draft.v1.json`
- `deliverable_grade`: `honest_null`
- Recheck with
  `python3 scripts/verify_erdos319_417_triage.py examples/erdos-problems/attack/erdos319-official-exact-triage-record-draft.v1.json`.

Finite exact table:

- `examples/erdos-problems/attack/erdos319-small-exact-table-record-draft.v1.json`
- `deliverable_grade`: `partial_proof`
- Exact enumeration gives `c(N)=0` for `1<=N<=5`, `c(N)=4` for
  `6<=N<=11`, and `c(12)=6`.
- Witness sign maps: `{1:+,2:-,3:-,6:-}` for the size-4 row and
  `{1:+,2:-,3:-,4:-,6:+,12:-}` for the size-6 row.
- Recheck with
  `python3 scripts/verify_erdos319_small_table.py examples/erdos-problems/attack/erdos319-small-exact-table-record-draft.v1.json`.

Lean exact finite table:

- `lean/Vela/Erdos/Erdos319.lean`
- `examples/erdos-problems/attack/erdos319-lean-small-table-record-draft.v1.json`
- `deliverable_grade`: `partial_proof`
- Lean defines an executable exact cleared-integer enumeration `cExact` and proves
  `cExact(N)=0` for `1<=N<=5`, `4` for `6<=N<=11`, and `6` for `N=12`.
- This is a kernel-checked version of the small finite table only. It is not the
  official all-`N` extremal result and does not replace the larger Python MITM
  table through `N=27`.
- Recheck with
  `cd lean && lake build Vela.Erdos319`
  and
  `python3 scripts/verify_erdos319_lean_small_table.py examples/erdos-problems/attack/erdos319-lean-small-table-record-draft.v1.json`.

Unit-fraction anchor bridge:

- `lean/Vela/Erdos/Erdos319.lean` now also defines `recip` and
  `anchorSignedRecip`.
- New Lean theorem `sum_recip_lt_of_ssubset`: positive reciprocal sums strictly
  increase under proper inclusion.
- New Lean theorems `proper_subsum_lt_one_of_unit_fraction` and
  `proper_subsum_ne_one_of_unit_fraction`: if a positive denominator set `B`
  has reciprocal sum `1`, every proper subcollection has reciprocal sum below
  `1` and therefore not equal to `1`.
- New Lean theorem `sum_anchorSignedRecip_eq_zero`: the anchored set `{1} ∪ B`
  has signed reciprocal sum zero when all elements of `B` are assigned negative
  sign.
- New Lean theorems `anchorSignedRecip_no_one_subsum_ne_zero` and
  `anchorSignedRecip_with_one_proper_subsum_ne_zero`: proper anchored
  subcollections have nonzero signed sum in the two cases needed by the
  Croot/Adenwalla lower-bound mechanism.
- New Lean theorem `anchorSignedRecip_proper_subsum_ne_zero`: direct minimality
  statement for every nonempty proper subcollection of `{1} ∪ B`.
- Draft record:
  `examples/erdos-problems/attack/erdos319-unit-fraction-anchor-bridge-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos319_unit_fraction_anchor.py`.
- Proposed grade: `partial_proof`.
- Caveat: this formalizes the structural lower-bound mechanism behind the
  known `Omega(N)` result. It does not answer the official exact all-`N`
  extremal question.

Official-shape anchor bridge:

- `lean/Vela/Erdos/Erdos319.lean` now also defines `SignedReciprocalAssignment`,
  `MinimalSignedZeroSum319`, and `OfficialAdmissible319`, a local predicate
  matching the official minimal signed zero-sum shape at cutoff `N`.
- New Lean theorem `anchorSignedRecip_assignment`: the anchored sign function
  chooses `+1/n` or `-1/n` on every element of `{1} ∪ B`.
- New Lean theorem `unitFraction_anchor_officialAdmissible`: if
  `B ⊆ {2,...,N}` and `sum_{b in B} 1/b = 1`, then `{1} ∪ B` with anchored
  signs is an official-shape admissible #319 set inside `{1,...,N}`.
- New Lean theorem `unitFraction_anchor_card`: the anchored construction has
  cardinality `|B|+1`.
- Draft record:
  `examples/erdos-problems/attack/erdos319-official-anchor-bridge-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos319_official_anchor_bridge.py`.
- Proposed grade: `partial_proof`.
- Caveat: this aligns the lower-bound construction with the official predicate.
  It does not prove extremality or the all-`N` answer.

Smoothness upper-bound mechanism:

- `lean/Vela/Erdos/Erdos319Smoothness.lean` formalizes the algebraic core of the
  known smoothness route to the upper bound.
- New definitions `sgnSum`, `Irreducible`, `Ap`, `Aq`, `Kpf`, `Tnum`, and
  `Unum`.
- New Lean theorem `Tnum_add_pUnum`: clearing denominators gives the integer
  identity `T + p*U = 0`.
- New Lean theorem `p_dvd_Tnum`: the prime `p` divides the cleared numerator
  `T` of the `p`-divisible part.
- New Lean theorem `Ap_signed_zero`: if the isolation bound `|T| < p` holds,
  then the `p`-divisible part is itself signed-zero.
- New Lean theorem `Ap_eq_self_of_irreducible`: if the original set is
  irreducible and `A_p` is nonempty, the same isolation bound forces `A_p=A`.
- New Lean theorem `not_isolated_of_irreducible_split`: if both the
  `p`-divisible part and `p`-free part are nonempty in an irreducible
  signed-zero relation, then the isolation bound `|T|<p` is impossible.
- New Lean theorem `split_prime_forces_large_Tnum`: equivalently, every prime
  that splits such a relation forces `p<=|T|`.
- Draft record:
  `examples/erdos-problems/attack/erdos319-smoothness-mechanism-record-draft.v1.json`.
- Split-prime barrier draft record:
  `examples/erdos-problems/attack/erdos319-split-prime-isolation-barrier-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos319_smoothness_mechanism.py`.
- Split-prime barrier verifier:
  `scripts/verify_erdos319_split_prime_isolation_barrier.py`.
- Counting corroboration:
  `scripts/verify_319_smoothness_counting.py` recomputes exact finite
  nonsmooth counts for `N=10^5,10^6,10^7`.
- Proposed grade: `partial_proof`.
- Caveat: the isolation estimate and the PNT counting step remain external
  analytic inputs. This is not a solve of the official exact all-`N` extremal
  problem and does not determine the sharp leading constant.

Extended finite exact table:

- `examples/erdos-problems/attack/erdos319-mitm-exact-table-record-draft.v1.json`
- `deliverable_grade`: `partial_proof`
- Exact meet-in-the-middle enumeration gives `c(N)=0` for `1<=N<=5`, `4` for
  `6<=N<=11`, `6` for `12<=N<=14`, `8` for `15<=N<=17`, `10` for
  `18<=N<=20`, `12` for `21<=N<=23`, and `14` for `24<=N<=27`.
- Recheck with
  `python3 scripts/verify_erdos319_mitm_table.py examples/erdos-problems/attack/erdos319-mitm-exact-table-record-draft.v1.json`.

Do not bank #319 as closed from the asymptotic-order variants.

## #417 status

Partial Lean bridge after #463. The prior easy observation `V'(x)<=V(x)` is now
formalized in the right finite-set language, and the gap is isolated exactly as
hidden first-preimage totients. The existence of the limit and the strict
inequality remain open.

Lean support:

- `lean/Vela/Erdos/Erdos417.lean`
- New definitions `domainTotientValues`, `boundedTotientValues`,
  `hiddenTotientValues`, `VprimeCount`, `VCount`, and `hiddenCount`.
- New Lean theorem `domainTotientValues_subset_boundedTotientValues`: the
  `V'(x)` finite set is a subset of the `V(x)` finite set.
- New Lean theorem `VprimeCount_le_VCount`: the official trivial inequality.
- New Lean theorem `mem_hiddenTotientValues_iff`: a value lies in the gap exactly
  when it is a totient value at most `x` and no preimage at most `x` realizes it.
- New Lean theorem `VCount_eq_VprimeCount_add_hiddenCount`: the exact finite
  decomposition `V(x)=V'(x)+hiddenCount(x)`.
- New Lean theorem `VprimeCount_pos`: the denominator `V'(x)` is positive for
  every `x>=1`.
- New definitions `VRatio` and `hiddenRatio`: rational versions of
  `V(x)/V'(x)` and `hiddenCount(x)/V'(x)`.
- New Lean theorem `VRatio_eq_one_add_hiddenRatio`: for every `x>=1`,
  `V(x)/V'(x)=1+hiddenCount(x)/V'(x)`. This pins the official ratio question to
  the normalized hidden first-preimage count, but does not prove a limit.
- New Lean theorem `tendsto_VRatio_of_tendsto_hiddenRatio`: convergence of the
  normalized hidden contribution implies convergence of the official ratio to
  `1` plus that limit.
- New Lean theorem `tendsto_VRatio_limit_gt_one_of_tendsto_hiddenRatio_pos`: if
  the normalized hidden contribution converges to a positive value, then the
  corresponding official ratio limit is strictly greater than `1`.
- New Lean theorem `self_mem_hiddenTotientValues_of_totient`: if `x>1` is
  itself a totient value, then `x` is hidden at cutoff `x`.
- New Lean theorem `VprimeCount_lt_VCount_prime_pred`: for every prime `p>2`,
  the point `x=p-1` has strict pointwise gap `V'(x)<V(x)`. This does not prove
  the official limit exists or that its value is strictly greater than `1`.
- New Lean theorem `exists_arbitrarily_large_strict_gap`: these prime-predecessor
  points give arbitrarily large cutoffs with `V'(x)<V(x)`.
- New Lean theorem `VRatio_gt_one_of_VprimeCount_lt_VCount`: any strict finite
  count gap gives the official rational ratio `V(x)/V'(x)>1`.
- New Lean theorem `VRatio_gt_one_prime_pred`: at every odd-prime predecessor
  `x=p-1`, the official ratio is strictly greater than `1`.
- New Lean theorem `exists_arbitrarily_large_VRatio_gt_one`: there are
  arbitrarily large cutoffs with `V(x)/V'(x)>1`. This is still not a limit
  theorem.
- New Lean theorem `VprimeCount_lt_VCount_of_totient_self`: every nontrivial
  totient value `x` gives a strict pointwise gap at cutoff `x`.
- New Lean theorem `VRatio_gt_one_of_totient_self`: therefore every cutoff `x`
  with `x>1` and `x=phi(m)` has `V(x)/V'(x)>1`.
- New definition `firstTotientPreimage`: the first input whose totient is `v`,
  or `0` if `v` is not a totient value.
- New Lean theorem `firstTotientPreimage_spec`: when `v` is a totient value,
  the first preimage really has totient `v`.
- New Lean theorem `firstTotientPreimage_le_of_totient`: every other preimage of
  `v` is at least as large as `firstTotientPreimage v`.
- New Lean theorem `mem_hiddenTotientValues_iff_firstPreimage_gt`: the hidden
  values are exactly the totient values `v<=x` whose first preimage lies above
  the cutoff `x`.
- Draft record:
  `examples/erdos-problems/attack/erdos417-totient-self-strict-ratio-record-draft.v1.json`.
- Independent verifier:
  `scripts/verify_erdos417_totient_self_strict_ratio.py`.
- Proposed grade: `partial_proof`.
- Caveat: this broadens the pointwise strict-ratio obstruction from prime
  predecessors to all nontrivial totient cutoffs. It still does not prove the
  limit exists or that any limiting value is greater than `1`.

Draft records:

- `examples/erdos-problems/attack/erdos417-limit-gap-triage-record-draft.v1.json`
- `deliverable_grade`: `honest_null`
- Recheck with
  `python3 scripts/verify_erdos319_417_triage.py examples/erdos-problems/attack/erdos417-limit-gap-triage-record-draft.v1.json`.
- `examples/erdos-problems/attack/erdos417-hidden-totient-decomposition-record-draft.v1.json`
- `deliverable_grade`: `partial_proof`
- Recheck with
  `python3 scripts/verify_erdos417_hidden_decomposition.py examples/erdos-problems/attack/erdos417-hidden-totient-decomposition-record-draft.v1.json`.
- `examples/erdos-problems/attack/erdos417-prime-pred-hidden-gap-record-draft.v1.json`
- `deliverable_grade`: `partial_proof`
- Recheck with
  `python3 scripts/verify_erdos417_prime_pred_hidden_gap.py examples/erdos-problems/attack/erdos417-prime-pred-hidden-gap-record-draft.v1.json`.
- `examples/erdos-problems/attack/erdos417-ratio-identity-record-draft.v1.json`
- `deliverable_grade`: `partial_proof`
- Recheck with
  `python3 scripts/verify_erdos417_ratio_identity.py examples/erdos-problems/attack/erdos417-ratio-identity-record-draft.v1.json`.
- `examples/erdos-problems/attack/erdos417-strict-ratio-points-record-draft.v1.json`
- `deliverable_grade`: `partial_proof`
- Recheck with
  `python3 scripts/verify_erdos417_strict_ratio_points.py examples/erdos-problems/attack/erdos417-strict-ratio-points-record-draft.v1.json`.
- `examples/erdos-problems/attack/erdos417-first-preimage-residual-record-draft.v1.json`
- `deliverable_grade`: `verified_reduction`
- Recheck with
  `python3 scripts/verify_erdos417_first_preimage_residual.py examples/erdos-problems/attack/erdos417-first-preimage-residual-record-draft.v1.json`.

Caveat:

This is not a solve. It does not prove that `lim V(x)/V'(x)` exists, that it is
strictly greater than `1`, or that it is infinite.

## Final checks for Opus

Before banking #302, rerun:

```bash
python3 scripts/erdos302_prefix_scip_decision.py examples/erdos-problems/attack/erdos302-base241920.checkpoint.json --prefix-size 139 --prior-r 66 --prior-r 67 --prior-r 67 --prior-r 67 --prior-r 68 --prior-r 68 --prior-r 69 --prior-r 69 --seconds 1200 --out /tmp/erdos302-base241920-prefix139-check.json
python3 -m json.tool examples/erdos-problems/attack/erdos302-base241920-prefix139-scip-monotone-bound-draft.v1.json >/dev/null
python3 -m json.tool examples/erdos-problems/attack/erdos302-base241920-prefix139-scip-monotone-bound-verify.v1.json >/dev/null
python3 -m json.tool examples/erdos-problems/attack/erdos302-base241920-prefix139-scip-monotone-bound-record-draft.v1.json >/dev/null
python3 scripts/verify_erdos302_tail_ceiling.py examples/erdos-problems/attack/erdos302-base241920-tail-ceiling-record-draft.v1.json
git diff --check
```

Before banking #319, rerun:

```bash
python3 scripts/verify_erdos319_mitm_table.py examples/erdos-problems/attack/erdos319-mitm-exact-table-record-draft.v1.json
python3 scripts/verify_erdos319_small_table.py examples/erdos-problems/attack/erdos319-small-exact-table-record-draft.v1.json
python3 scripts/verify_erdos319_lean_small_table.py examples/erdos-problems/attack/erdos319-lean-small-table-record-draft.v1.json
python3 scripts/verify_erdos319_unit_fraction_anchor.py examples/erdos-problems/attack/erdos319-unit-fraction-anchor-bridge-record-draft.v1.json
python3 scripts/verify_erdos319_official_anchor_bridge.py examples/erdos-problems/attack/erdos319-official-anchor-bridge-record-draft.v1.json
python3 scripts/verify_erdos319_smoothness_mechanism.py examples/erdos-problems/attack/erdos319-smoothness-mechanism-record-draft.v1.json
python3 scripts/verify_erdos319_split_prime_isolation_barrier.py examples/erdos-problems/attack/erdos319-split-prime-isolation-barrier-record-draft.v1.json
python3 scripts/verify_319_smoothness_counting.py
cd lean && lake build Vela.Erdos319Smoothness
cd lean && lake build Vela.Erdos319
```

Before banking #463, rerun:

```bash
cd lean && lake build Vela.Erdos463
rg -n "sorry|axiom|unsafe|admit" lean/Vela/Erdos/Erdos463.lean lean/Vela.lean
python3 scripts/erdos463_margin_search.py --verify examples/erdos-problems/attack/erdos463-margin-prefix-10m-draft.v1.json
python3 -m json.tool examples/erdos-problems/attack/erdos463-margin-prefix-obstruction-record-draft.v1.json >/dev/null
python3 scripts/verify_erdos463_semiprime_bridge.py examples/erdos-problems/attack/erdos463-semiprime-gap-bridge-record-draft.v1.json
python3 scripts/verify_erdos463_factorial_obstruction.py examples/erdos-problems/attack/erdos463-factorial-offset-obstruction-record-draft.v1.json
python3 scripts/verify_erdos463_lcm_obstruction.py examples/erdos-problems/attack/erdos463-lcm-offset-obstruction-record-draft.v1.json
python3 scripts/verify_erdos463_official_window_bridge.py examples/erdos-problems/attack/erdos463-official-window-bridge-record-draft.v1.json
python3 scripts/verify_erdos463_balanced_window_coprime.py examples/erdos-problems/attack/erdos463-balanced-window-coprime-obstruction-record-draft.v1.json
python3 scripts/verify_erdos463_shifted_sieve_criterion.py examples/erdos-problems/attack/erdos463-shifted-sieve-criterion-record-draft.v1.json
python3 scripts/verify_erdos463_ceil_prime_semiprime_criterion.py examples/erdos-problems/attack/erdos463-ceil-prime-semiprime-criterion-record-draft.v1.json
python3 scripts/verify_erdos463_limsup_margin.py examples/erdos-problems/attack/erdos463-limsup-margin-record-draft.v1.json
python3 scripts/verify_erdos463_f_witness_bridge.py examples/erdos-problems/attack/erdos463-f-witness-bridge-record-draft.v1.json
```

Before banking #417, rerun:

```bash
python3 scripts/verify_erdos417_hidden_decomposition.py examples/erdos-problems/attack/erdos417-hidden-totient-decomposition-record-draft.v1.json
python3 scripts/verify_erdos417_prime_pred_hidden_gap.py examples/erdos-problems/attack/erdos417-prime-pred-hidden-gap-record-draft.v1.json
python3 scripts/verify_erdos417_ratio_identity.py examples/erdos-problems/attack/erdos417-ratio-identity-record-draft.v1.json
python3 scripts/verify_erdos417_strict_ratio_points.py examples/erdos-problems/attack/erdos417-strict-ratio-points-record-draft.v1.json
python3 scripts/verify_erdos417_first_preimage_residual.py examples/erdos-problems/attack/erdos417-first-preimage-residual-record-draft.v1.json
cd lean && lake build Vela.Erdos417
rg -n "sorry|axiom|unsafe|admit" lean/Vela/Erdos/Erdos417.lean lean/Vela.lean
```
